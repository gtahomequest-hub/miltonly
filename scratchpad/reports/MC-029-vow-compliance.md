# MC-029
CORE · D:\miltonly · fix/vow-compliance

1. Branch `fix/vow-compliance`, head **`6c23bdc`** (two commits on `e606d8b`: `486ff2a` the work, `6c23bdc` the door sign-in and the status strip). Local gate on Node 22 exit 0, 168/168, twice. **Not merged; waits on approval.**
2. Preview **`https://miltonly-czlzy3wc7-gtahomequest-hubs-projects.vercel.app`** serves `6c23bdc`; full battery **`PASS · 21 checks · 609 pages · 769s`** (`scratchpad/mc003/battery-mc029-preview-6c23bdc.log`). The new check is red on production at `895962b`: 16 of 16 anonymous surfaces carry a VOW-only field, 8 of 15 assertions fail (`battery-mc029-prod-red.log`).
3. VOW-only fields off every public surface: `src/lib/listings/vow.ts` names the seven columns (`daysOnMarket`, `listedAt`, `priorPrice`, `priceChangedAt`, `lastPriceChangeAt`, `soldPrice`, `soldDate`) and `stripVowFields` runs on every Prisma row before it is serialised for a client component; the listing page's meta description and header no longer say "Listed N days ago" or "Nd on market"; cards on `/listings`, `/rentals`, `/rent`, the street tiles, the home cards, the school, mosque and condo cards, the menu panels and the ad pages carry no day count, list date, prior price, sold price or status; the Buy menu's "Price changes" panel keeps the count and shows the newest four homes instead of the changed ones. A signed-in, acknowledged session reads the facts from `/api/listings/[mls]/vow` (force-dynamic, `getSession` + `vowAcknowledgedAt`) through the island on the listing page, and on `/listings` (force-dynamic) the page reads the session on the server and the loader selects the columns only then. Sold, expired and leased listing pages answer the display-flag shell, noindex, the same words for every reason; `/listings?status=sold` redirects to `/sold`; `/api/auth/saved-listings` answers "active" or "unavailable" to an unacknowledged session.
4. Public-display permission: every public query goes through `PUBLIC_SALE_WHERE`, `PUBLIC_LEASE_WHERE` or `PUBLIC_LISTING_WHERE` (`permAdvertise` AND on the market, sale by `status`, lease by `leaseStatus`), or checks `isPublicListing` on the row; `/rentals`, `/rent`, `/rentals/ads`, the ad landing pages and the condo page selected leased units before, and the sales ad page never checked the flag. Both sitemaps already carried the flag and `status = active`; unchanged. The battery looks every `/listings/<mls>` in the live sitemap (470) and every MLS number the public grids link up in DB1: 0 without the flag or off the market, on production and on the preview.
5. Brokerage prominence (item 27): `src/components/listings/ListingBrokerage.tsx`, one component, placed inside the `[data-price]` element on the grid card, the listing page, the street tile, the home card, the menu card, the rentals card, the school, mosque and condo cards, the ad pages and the slider, so it inherits the price's face, size, weight and colour; beside the price in the alert email at the price's size, weight and colour. Measured in a real browser on five surfaces: 0 mismatches, 0 surfaces without one.
6. Battery check `vow-fields` (`scripts/verify/checks/vow-fields.mjs`): 16 anonymous surfaces, the three off-market shells, the sold redirect, the gated route both ways, the grid's cards both ways, the island in a browser, the brokerage measurement, the flag lookup. The signed-in half goes through the real door (`JWT_SECRET` is a sensitive Vercel secret and cannot be pulled): a sign-in request for the desk's acknowledged user, the code read off the row, verified, the cookie cached per host in the OS temp dir. One sign-in email per fresh sign-in; the door's limiter (three an hour per address, five per IP in ten minutes, shared with the lead forms) fails the check by name when hit. Prebuild `scripts/test-vow-fields.ts`, 67 assertions, red on main's tree.
7. Aggregates kept, listed: a street's typical days on market (k5, DB3) and its "Time on market" cells; the hub's and the homepage's days on market; the market edition's "after N days on market" and the Sell menu's "days to sell"; the menu's "N price changes in the last week" and "N listed in the last 24 hours"; `/rentals` and `/rent` "N new this week" (now counted on the server); `/api/street-stats` (k-floored DB1 averages); `/api/listings/count`; the daily brief's counts and street events. Not public, unchanged: the bearer-token `/api/content/v1/*` routes, which return `listedAt` per listing to the content engine.
8. Deleted: `/listings-v2-preview` and `src/components/listings/v2/mockData.ts` (a public route rendering fabricated sold cards), `ListingsCardsClient.tsx`, `ListingsGrid.tsx`, `components/street/ActiveInventory.tsx` (all dead). `test-lead-forms.ts` and `test-neon-egress.ts` updated for the deletion and the `/rentals` cache key (`v2`, the bundle now carries `newThisWeek`).
9. URLs changed, for the TRREB reply: `/listings/<mls>` (every listing page: 470 active sale and 1,179 available lease pages lose the day count, list date, price history and status; 604 sold, 1,044 expired and 229 leased pages now answer the not-available shell, noindex); `/listings`, `/listings?status=rent`, `/listings?page=N`; `/listings?status=sold` (redirects to `/sold`); `/rentals`, `/rentals?neighbourhood=<hub>`, `/rent`; `/rentals/ads`, `/rentals/ads/<mls>`, `/sales/ads/<mls>`; `/streets/<slug>` (609, the tiles); `/` (the newest cards); `/condos/<slug>`, `/schools/<slug>`, `/mosques/<slug>`; every page's Buy menu (the price-change panel and the cards); `/api/listings/<mls>/vow` (new, gated); `/api/auth/saved-listings`; the deal-alert email; `/listings-v2-preview` (removed).
10. Left on production by the proofs: the desk's user has a cleared `verifyCode` and two sign-in emails in its inbox. Stopped here; merge on approval (`git merge --no-ff 6c23bdc`, the local gate, push, `npx vercel ls --prod`, the battery with the merge SHA).

Report: scratchpad/reports/MC-029-vow-compliance.md
