# MH-003
HOME · D:\miltonly-home · feat/menu-v2

**Preview:** https://miltonly-p5moxdfdn-gtahomequest-hubs-projects.vercel.app (alias `miltonly-git-feat-menu-v2`)
**Head SHA: `3ec8b511bc40b3de005ffeeb12f1d9bffc6708f4`**, not merged. Commits after it are docs only.

- The rail is a `role="tablist"`: each item a `<button role="tab">` that selects its own panel on hover with the same 120ms intent (mouse only), on focus (Tab, ArrowUp/Down, Home/End) and on click; the first item is selected by default. Every item's panel is in the served HTML on every page, one visible, each with at least one live block and its CTA. At 380 each item is a nested `<details>`, the first open, a tap opens the rest inline. An "Also" row in every foot keeps the destinations the rail no longer names.
- Every panel is live, from one composer (`src/lib/megaLive.ts`) through `figureFormat`: new today (24h / week / for sale, newest four cards), price changes (`lastPriceChangeAt`, 7 days or 30 when the week has none, prior price and direction only where DEC-PRICE-HISTORY observed the change), condos and freehold (the tenure hubs' own sub-type sets), rentals (`leaseStatus='active'`), alerts (this week's counts + the daily-brief signup via `postLead`), by neighbourhood (22 hubs with counts), with video (eight posters), A to Z (published pages per letter, `/streets?letter=X`, which the directory now honours), address search (+ most-searched strip), what's it worth (Board row, window and sample), sold this month (k-gated typical, "so far"), market watch (the latest published edition's counts and sentence). **Open houses are not built**: the feed carries no open-house field on any row, so a panel of them could only be invented.
- Gate: `nav.mjs` declares a format for every `menu-` figure and fails on an unknown one; statically asserts per menu one selected tab, one visible item panel, none empty, on five page types; in Chrome at 1024 and 1440 hovers every rail item and asserts selection, exclusivity, live content, CTA, viewport fit and no internal scroll, then ArrowDown focus-selection; at 380 asserts the first item open by default and a tap opening each of the rest, none empty. `homepage.mjs` adds `menu-buy-rentals` and `menu-sold-mtd`.
- `pnpm build` exit 0 at `3ec8b51`, zero `P2024`, 556 static pages. Battery on the preview at `3ec8b51`: **`FAIL · 14 checks · 463 pages · 260s`**, 2 assertions, both one figure: `sold-mtd` (the homepage's OWN hero tile, untouched by this branch) and its menu copy `menu-sold-mtd` print 51 against 59 live. Production prints 51 too; the app caches `getSoldThisMonth` an hour in the shared Upstash and an app instance wrote 51 at 15:52 while a direct query of the same DB2 host returned 59. Pre-existing, isolated to that figure, owned by the homepage's data path, not the menu. Every nav assertion and the other 13 checks PASS. Local `--only=nav` PASS at the same SHA.
- Left as is: the footer's `/book` link (a 307). Screenshots of every rail item at 1440 and 1024 and two at 380 are in `scratchpad/menu-v2/` (untracked).

Report: scratchpad/reports/MH-003-rail-drives-panel.md
