# MC-021
CORE · D:\miltonly · docs/MC-021-reporting

CLAUDE.md Reporting: the `code D:\miltonly <that path>` step and its worktree-root list are removed. A task now ends by writing `scratchpad/reports/<TASK-ID>-<slug>.md` (first line `# <TASK-ID>`, tracked in git) and a reply whose last line is `Report: <path>`; nothing opens the editor. Docs only, no app file changed.

HANDOFF.md carries a one-line note, QUEUE.md an out-of-queue entry. Branch `docs/MC-021-reporting` off local main (`20aa2d7`), pushed. Not merged: local main is still 3 commits ahead of `origin/main` (the MA-003 merge), and MC-020 on `fix/audit-night-1 @ 4a31b1d` is still open with no report or HANDOFF rewrite.

Report: scratchpad/reports/MC-021-reporting-no-editor.md
