# MC-015
CORE · D:\miltonly · feat/video-playbook

**Built on `feat/video-playbook`, app code `b090002`, the docs commit on top is the head. CLI preview `miltonly-3d306kdaz` serving `b090002`; battery on the preview `EXPECT_SHA=b090002`: `PASS · 18 checks · 529 pages · 720s`.** The 18th is `video`: 45 pages, 45 clips, 90 objects HEADed, 45 listed with a duration, 44 sentences stating metres, 28 both endpoints, all 13 assertions 0. Local build exit 0, 149/149; `test-video-playbook` 50 assertions in the prebuild. **Waiting on approval to merge.** Every data move is already live on production, in the queue's order; the sentence, the takedown line, the sitemaps and `VideoObject.duration` wait for the merge.

1. **Dated keys, live.** All 47 clip-carrying rows point at `streets/<slug>-milton/<YYYYMMDD>/{day,night}.mp4` with `poster.webp` beside each (`scripts/rekey-video-dated.ts`: server-side copy, size-verified, repointed, revalidated). `retire-superseded-clips.ts --write` deleted the 90 old objects after seeing production serve every dated URL, page by page; the old keys 404. Manifest rebuilt, meta stamped; the upload script writes dated keys only. Spelling is MC-007's segment (`<YYYYMMDD>/day.mp4`), which two clips and `deriveVideoPoster` already used.
2. **Capture times, rewritten from the GPS trace: 47 rows changed, 41 by about an hour.** The camera's filename clock runs one hour fast (raw `2026_0901_193438` opens at GPS 22:34:41Z, 18:34 in Milton), and every clip published before 2026-09-11 took `captured_at` from the filename. `scripts/video-coverage.ts` set each clip's `captured_at` to the GPS row of its first trace point on the street (`britannia-road` from the file's first row; that stretch is the Region's, not in the Town layer), then `backfill-video-captured.ts --write` wrote the 47 instants. Captions render in America/Toronto. **Three "night" clips were daylight**: `clifford-point`, `first-line`, `frost-court` were filed at 20:15 to 20:21 on the fast clock, the GPS says 19:15 to 19:21 and their mean luma (YAVG 114) is the daytime clips' figure; relabelled day (`--to-day`: copied to `<seg>/day.mp4`, `videoUrl` takes the pointer, night columns cleared). The offset columns I added at 20:00Z were withdrawn by a second migration before any row carried one (status clean, 29).
3. **`sitemap-video.xml`** (title, description, thumbnail, contentUrl, duration, publication date with the Toronto offset), 45 pages / 45 clips; **`sitemap-index.xml`** names it beside `/sitemap.xml` and `robots.txt` names the index (`/sitemap.xml` itself unchanged, so the battery and the audit read what they always read); `VideoObject.duration` on every clip. Search Console submission after the merge is a human step.
4. **The coverage sentence**, under every player and as `VideoObject.description`: "Footage covers 460 m of 510 m, from Leiterman Drive to Parmenter Point. Filmed 1 September 2026." Metres are the GPS run projected onto the street's Town centreline (capped at the street; a close is driven in and out) over `streetGeometry` length; endpoints are the registry streets whose Town segment meets the run within 40 m, named by `resolveStreetName`. 46 of 47 with metres, 30 with both endpoints, 14 with one ("from A"), 3 with none ("Footage is one pass along <Street>"). A clause with no source is dropped, never filled. Sidecar `src/data/streetVideoMeta.json`, rebuilt by the backfill script.
5. **Takedown mailto** on every page with footage: `config.video.takedownEmail` = `aamir@miltonly.com`.
6. **Upload script** refuses an audio stream (ffprobe, `scripts/videoProbe.ts`; ffprobe missing refuses the run) or `blur_verified` false, naming both; also refuses a `captured_at` without an offset. Not exercised on a real candidate: the 124 staged rows are Homesly, all `blur_verified: false`.
7. **`db3` after an analytics run**: live since MC-017 (the three jobs call `revalidateTag(DB_CACHE_TAG.ANALYTICS_DATABASE_URL)`), held by `test-build-cost.ts`.

**After the merge:** one `retire-superseded-clips.ts` dry then `--write` (the three dated `night.mp4` objects), Search Console. **Left alone:** `louis-st-laurent-avenue` and `lower-base-line-west`, published clips with no page. **For the dashcam side:** its GPS rule was right and the 40 clips that predate it carried the filename clock into production for ten days; `meta.json` now records `captured_at_filename`, `captured_at_source`, `captured_end`, `coverage_source`, `relabelled`.

Report: scratchpad/reports/MC-015-video-playbook.md
