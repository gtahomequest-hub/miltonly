# ML-003

LEADS · D:\miltonly-leads · feat/leads

1. **Built.** `/api/digest/leads` mails the desk one report: leads by source and by page for the 7 and the 28 local days ending Sunday (from `lead_daily_by_page`), brief subscribers and watches active, confirmation and desk-alert delivery counts, and every mounted surface with no submission in 28 days. Two UTC crons, `0 11 * * 1` and `0 12 * * 1`; the route runs only at Toronto hour 7. Recipient `LEADS_DIGEST_TO`, falling back to `ALERT_EMAIL_TO` (`gtahomequest@gmail.com` in both environments), so production needs no new variable.
2. **Bases.** The view's `day` is a date and is bounded by dates; `SavedSearch` and `LeadActivity` timestamps are read on the Toronto instants. The view itself buckets by UTC day (Phase 1 migration); the digest states that in its footer and the route returns a cross-check on the instants, which agreed (10 and 11) on 2026-09-11. The one-line view fix is Core's migration, recorded as open.
3. **Delivery counts had no source.** Neither send left a trace. `recordDeliveries` now writes one `LeadActivity` row per attempt (`email_sent` + Resend id, `email_failed` + message); skipped sends leave none, a log failure never costs a lead. Proven on preview: lead `cmtxmrdy70000glan0oblr6e7` produced two `email_sent` rows whose ids match the diagnostics.
4. **Proof send.** `LEADS_DIGEST_TO=gtahomequest+ml003@gmail.com` set on Preview, one `force=true` send from `miltonly-bsn6qltes`: Resend `118ed5c7-b5fa-455f-8991-a5fc15e7ca94`, received in the inbox at 23:58Z with subject `[preview] Leads digest, week to 2026-09-10`. The variable was then deleted; it exists nowhere now.
5. **Prebuild.** `scripts/test-leads-digest.ts`, 228 assertions: the window on both bases including the clock-change week, the slot in EDT and EST, `src/lib/lead/sources.ts` (43 live, 4 retired) held to mounted files under `src/`, the compose read off the source, the delivery log, the route. Proven to fail on a phantom surface.
6. **Gates.** `origin/main` moved twice mid-task; merged (`2a89120`, prebuild line unioned), rebuilt. Local `pnpm build` exit 0. Battery on the preview at `e20575701ff0523834da424fe043a0b489192e49`: first run failed one homepage neighbourhood figure by $5k against its live hub record, a sold-figure cache timing outside the lead layer that production also renders; the re-run passed, and the full re-run is **PASS · 14 checks · 481 pages · 394s**.
7. **For Core to merge by SHA: `e20575701ff0523834da424fe043a0b489192e49`** is the proven tree; the head is one docs commit above it carrying this report, the handoff and the queue mark. Merge the head. First production digest: Monday 2026-09-14, 11:00 UTC, if deployed by then.

Report: scratchpad/reports/ML-003-weekly-leads-digest.md
