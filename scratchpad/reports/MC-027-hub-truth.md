# MC-027
CORE · D:\miltonly · fix/hub-truth

**Built on `fix/hub-truth`, app code `f415ae4`, scripts `7f2e798`, the docs commit on top is the head. CLI preview `miltonly-6e2ddgzp7` serving `7f2e798`; battery on the preview `EXPECT_SHA=7f2e798`: `PASS · 20 checks · 589 pages · 679s`**, with the battery's own record moved to the median. Local build exit 0, 171/171 (the 22 hubs now prerender). **Waiting on approval to merge.**

1. **Drift, and the 22 hubs regenerated.** `src/lib/hubDataHash.ts` is the hub twin of `calcMarketDataHash` (typical to $10,000, sale count, days on market to the day, active count); both generators write it as `HubGeneration.inputHash`; `src/lib/hubDrift.ts` compares it with the live input from `getHubInputCached`, the same computation the page renders from, so the validator that grounds the new prose reads the same figures. The hub page emits FAQPage only when not drifted. `/api/sync/regenerate-hubs` is on the crons at 12:45 UTC: three drifted hubs a run, DeepSeek only, the Opus fallback refused (`deepseekOnly`). All 22 were drifted (their stored hash predates the rule), and **all 22 were regenerated tonight on DeepSeek for $0.109** (`scripts/regenerate-hubs-drift.ts`; cobban failed closed twice on the compared-to-Milton half and passed on the third run, dorset-park once; the preview's dry run now reports 0 drifted). Production keeps its June prose in its day cache until its next tag drop or the merge; it was not purged, because the new prose is grounded on the median and production's tiles are still the mean.
2. **Prerender and warm.** `generateStaticParams` returns the published hubs; `/api/jobs/warm-hubs` walks the 22 paths and runs at 11:12, 11:42, 12:12 and 12:55 UTC, after the sold sync, the analytics jobs and the regenerate run. **First-visitor TTFB on the preview: purged, as a tag drop leaves a hub, 2.9 s, 3.1 s and 5.6 s (Beaty, Ford, Timberlea); prerendered, 22 of 22 `PRERENDER`, p50 0.24 s, max 0.36 s.** The warm route walked the 22 in 5 s.
3. **DEC-TYPICAL-MEDIAN.** Every `AVG(sold_price)` that fed a "typical" is `PERCENTILE_CONT(0.5)`: the street page (12-month, full-window, lease, per-bed rents), the street and hub AI inputs (aggregate, quarterly trend, by-type, the Milton-wide context), `neighbourhoodLookup`, `streetEnrichment`, `tenureHubData`, the condo input, the sibling cards (now the hub's own `saleAggQuery`), the ladder (each slug's sorted price array pooled per identity, `medianOf`), and the battery's record (one median query per hub over its raw strings). `/sold` lost its "Average sold price" tile, its table says "Typical sold price", it names the hub in the H1 and title under `nbhd`, shows a chip for all 22 published hubs, and its sign-in redirect carries `type`, `nbhd` and `ptype` (`/signin?redirect=%2Fsold%3Ftype%3Dsale%26nbhd%3Dtimberlea`). The one mean that stays is the "Typical is not average" guide's, computed as a mean and named as one. **Post-merge step:** `scripts/queue-mean-typical-streets.ts --write` queues the 40 published street pages whose stored prose states the old mean (median differs at $5,000); it waits for the merge because the hourly cron generates with production's code. DB3's `avg_sold_price` and the sold-stats API still say average and stay means.
4. **Bronte Meadows is an urban hub** (`Neighbourhood.profile` in DB1, the seed, `hasVipTier`), regenerated as one; its title reads "Street Guide". A polygon-less rural hub's siblings are the other rural seeds in seed order (Nassagaweya: Rural Trafalgar, Brookville / Haltonville, Campbellville, Moffat; Moffat: Nassagaweya, Rural Trafalgar, Brookville / Haltonville, Campbellville); neither thin-urban hub appears there now.
5. **Membership from the registry only.** The street page's up-link reads `ResidentialStreet.neighbourhoodId` (the street or a sibling slug), published hubs only; the sold strings no longer decide (Whitlock Avenue and Laurier Avenue now link to Cobban and Timberlea, their ladders). `scripts/hub-membership-reconcile.ts` over 590 published pages: 551 agree, 33 have no sales string, **1 fixed** (`geddes-landing` took Scott; the registry was empty), **4 conflicts listed, not changed**: `maple-avenue` (registry Dorset Park; 26 records Dempsey, 9 Dorset Park), `20-side-road`, `old-tremaine-road`, `tremaine-road` (one or two records each way); `15-side-road-side-road` has no registry row. Maple Avenue is the one worth a look at the map.

**Prebuild:** `scripts/test-hub-truth.ts`, 44 assertions; `test-build-cost` taught that the hub page prerenders. **Also:** `scripts/_server-only-shim.cjs` stands in for `React.cache` in scripts. The first `npx vercel deploy` after a pause answered "Not authorized" and the retry worked, twice now.

Report: scratchpad/reports/MC-027-hub-truth.md
