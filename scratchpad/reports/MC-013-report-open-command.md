# MC-013
D:\miltonly · main

## MC-013: the report open command names the repo window

Docs-only. One line in `CLAUDE.md`, under **Reporting**: `code <that path>` becomes `code D:\miltonly <that path>`, so the report opens in this repo's editor window rather than whichever window last had focus; a worktree uses its own root (`D:\miltonly-home`, `D:\miltonly-leads`, `D:\miltonly-content`). Nothing else in the file moved. HANDOFF.md and QUEUE.md unchanged.

Report: scratchpad/reports/MC-013-report-open-command.md
