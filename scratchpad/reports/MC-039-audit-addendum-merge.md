# MC-039

CORE · D:\miltonly · main

(No task ID in the prompt; MC-039 is the next in Core's sequence.) `feat/audit @ 9603468` merged by SHA as **`c1565b7`** and pushed 20:13:55Z. **The ignore rule did NOT skip the production build, and that is the rule working as written, not a fault.** Everything else is as you described.

1. **The diff carries nothing under `src/` or `prisma/`**, confirmed before I skipped the local build: seven files, `scripts/audit/morning/{config.json, rules.json, run.mjs, sources/db.mjs, sources/vercel.mjs}`, `scratchpad/reports/MA-008-morning-report.md` and `HANDOFF-audit.md`. Zero matches for `src/`, `prisma/`, `package.json`, `pnpm-lock.yaml`, `next.config.mjs`, `vercel.json`, `public/`. **`.github/workflows/morning-report.yml` is not in this addendum** (your note mentions the workflow; the file is unchanged since the first merge). No local build run, per your instruction; no battery either, since the app tree is byte-identical to the one `PASS · 24 checks · 708 pages` verified at `31da4b9`.
2. **The addendum is what it says.** `teamCapUsd` 200 → 300 with the `$comment` dated; the projection is now `spend + dailyRate × daysLeft`, where `dailyRate` is the mean of the last seven *full* days (`perDay.slice(0,-1).slice(-7)`), falling back to the cycle average only if that window is empty, and `daysToCap` divides by the same rate; `capRisk` is now `daysToCap <= daysLeft`, so a projection inside the cycle is the only thing that raises the flag. `sources/db.mjs` drops the `.slice(0, 10)` so the backlog is whole, and `rules.json` says "the top of the backlog listed in section 3".
3. **The rule built it.** Before pushing I ran the rule's own test against the served SHA: `git diff --quiet 31da4b9 HEAD -- . ':!scratchpad' ':!docs' ':!*.md'` exits **1** on five files, every one under `scripts/audit/morning/`. The rule excludes `scratchpad/`, `docs/` and `*.md` and nothing else, so anything under `scripts/` builds, exactly as CLAUDE.md states. Deployment `miltonly-gepjcuv36` is **Ready**; production serves `c1565b7` (`builtAt 2026-09-22T20:14:23Z`), identical app code to `31da4b9`. Two or three billed minutes. I considered cancelling it mid-build and did not: the build was already running, and letting it finish keeps `main` and production on the same SHA, which is worth more than the minutes.
4. **The durable fix, if you want audit-only pushes to skip.** Adding `':!scripts/audit'` to the pathspec takes the `ignoreCommand` from 250 to 268 characters and Vercel's limit is 256, so it does not fit inline; the way to do it is to move the logic into a repo file and set `"ignoreCommand": "bash scripts/vercel-ignore.sh"` (35 characters), which also makes the rule readable and testable. One small task, not done here.
5. **Today's 06:00 run had already fired before the merge**, on the old code: `report(morning): 2026-09-22` (`f50bfba`, report code `b3bced3`) says cycle to date $167.17, projected **$250.75**, headroom $32.83 against the **$200** cap, "crossed in 3.9 days", and prints one street ("Clarriage Crt E had 4 sales in 90 days"). Tomorrow's 06:00 run is the first on the addendum and should read about $213.95 projected, $134.45 headroom, no cap risk, and the 32-street backlog. Not dispatched by hand, as you asked.
6. **One line to expect in tomorrow's report anyway:** "Web Analytics: awaiting first data". The beacons are live and proven on production (MC-038), but MA-008's analytics source has no documented endpoint to read and the dashboard endpoint it tries answers 404; that line stays until Vercel exposes an API or Audit switches the source. Nothing to fix in the app.

Report: scratchpad/reports/MC-039-audit-addendum-merge.md
