# Merged and live. `de7f70b`. Battery 9/9 on production — including hub-meta.

## 1-2. Merge and deploy

```
33bed68 -> de7f70b   merge --no-ff fix/verify-build-sha    4 files, +99/-2
pushed 33bed68..de7f70b
```

Tree verified identical to `fix/verify-build-sha`, which built green, so I did not rebuild it.

**Production: `miltonly-rf40l1v4x`, Ready, 2m build.** And the SHA is confirmed from the deployment itself, not from git:

```
$ curl -s https://miltonly.com/api/build
{"commit":"de7f70b26358e7f2c5ebba17a8f83a61ca1cabe6"}
```

## 3. Battery against production — PASS, exit 0

```
build       de7f70b served == expected
sitemap     428 published street pages (derived, not a literal)
crawled     428 pages · 428 × 200 · 0 other
ASSERT iterated == live sitemap count (428) : PASS
═══ PASS · 9 checks · 428 pages · 64s ═══
```

The gate line is the new one, and it ran before the sitemap fetch.

| check | result |
|---|---|
| No sentence denies or contradicts a figure the page publishes | PASS — 8/8 assertions, 3,404 prose blocks, 10,148 schema strings |
| Structured data publishes exactly what the page publishes | PASS — 910 visible FAQ items == 910 schema Question nodes |
| Absence claims match the record, every page has a working CTA | PASS — 9/9, claimers 24 == derived zero-sale set 24 |
| Every published figure is floored against its own sample | PASS — 12/12 |
| One metric, one number | PASS — 7/7 |
| Every published page is a street that exists | PASS — 425 registry + 3 allowlist = 428 |
| Every published coordinate is real | PASS — 402 pages with distances, all attributed |
| **Hub meta, body and JSON-LD all publish the live aggregate** | **PASS — 11/11, all four mismatch counters 0** |
| Town polygons reproduce the TREB assignments | PASS — 97.5% over 713 streets, floor 95% |

**hub-meta passing 0/0 closes out Task 1.** `meta price != live typical: 0`, `meta sale count != live: 0`, `hero sold count != live: 0`, `JSON-LD price != live: 0`. There was never a data defect — it was stale cached renders, exactly as the recon concluded.

Three NOTEs, none gated and none new: ISR lag on 0 pages, the known rent-pill defect on 2 pages (`melville-bonus-crescent`, `mcdougall-crossing`), and stored `HubContent.metaDescription` drifting on 21 of 22 hubs — that column is no longer served.

## 4. The two one-liners you asked for

**Lockfile Vercel detected:** `2026-09-03T19:52:14.074Z Detected 'pnpm-lock.yaml' 9 which may be generated by pnpm@9.x or pnpm@10.x` — **production builds with pnpm.**

**packageManager field:** **absent** from `package.json`, which is why Vercel falls back to lockfile detection and picks pnpm.

So your "pnpm build is the gate" rule matches Vercel exactly, and my local `npm run build` is a different dependency resolution than what ships. Both lockfiles are committed and nothing pins the choice. Adding `"packageManager": "pnpm@9.x"` and dropping `package-lock.json` would make local and CI agree — your call, not done.

## 5. Nothing unpushed

Working tree clean, no stashes, and **all 13 local branches in sync with their upstreams**: `main`, `feat/name-source`, `feat/seo-crawl-budget`, `feat/street-adjacency`, `feat/street-meta-ctr`, `feat/street-video`, `fix/ai-provider-failclosed`, `fix/generator-name-wire`, `fix/name-prose`, `fix/provider-sitemap`, `fix/signin-unblock`, `fix/street-name-canon`, `fix/verify-build-sha`.

```
$ git log --branches --not --remotes --oneline
(empty)
```

## One thing I found and did not fix

The page count moved 427 -> 428 between my last run and this one. The new page is `gifford-crescent-milton`, published by the cron at 11:02 today — and it stored `streetName = "Gifford Cres"` while the registry says `Gifford Crescent`.

**The create branch of the upsert bypasses the naming authority.** `generateStreet.ts:615` writes the bare `streetName`; only the update branch got `resolveStreetName(...)` in Build 1. So every *newly created* StreetContent row still takes whatever MLS last wrote — the exact class of bug DEC-NAME-SOURCE was meant to close, surviving on the one path a regeneration never exercises.

Not user-visible today: the render layer and `buildGeneratorInput` both resolve, so the live page reads "Gifford Crescent" in title, H1 and prose, with zero abbreviated occurrences. The wrong value sits in the column, latent, waiting for any reader that trusts it directly. The prebuild guard missed it because it asserts the *file* imports `resolveStreetName`, which it does — just not on this path.

Stopping here as instructed. Say the word and it is a one-line fix plus a guard that checks the call site rather than the import.

## State

| | |
|---|---|
| main | `de7f70b`, pushed |
| production | `miltonly-rf40l1v4x`, Ready, serving `de7f70b` |
| battery | **PASS · 9 checks · 428 pages · 64s**, exit 0 |
| hub-meta | **PASS**, all mismatch counters 0 |
| Vercel package manager | pnpm (`pnpm-lock.yaml` detected); no `packageManager` field |
| branches | 13/13 in sync, nothing unpushed, no stashes |
| open | create-branch name bypass at `generateStreet.ts:615`; `parkway-drive` and `burnhamthorpe-road` still blocked |
