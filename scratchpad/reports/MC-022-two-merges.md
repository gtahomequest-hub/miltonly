# MC-022
CORE · D:\miltonly · main

**(1) MC-015 merged.** `feat/video-playbook @ d88742c` by SHA as `60780ca` (app code `b090002`), pulled first (`main` was `3ba3d91`, no nightly commit), local build exit 0, 149/149, `migrate status` clean; production `miltonly-r395cs4ih` served it, battery **`PASS · 18 checks · 529 pages · 668s`**. Confirmed on production: `sitemap-video.xml` 200 with 45 `<video:video>` entries, `sitemap-index.xml` names both files, `robots.txt` names the index, `/streets/attenborough-terrace-milton` shows "Footage covers 460 m of 510 m, from Leiterman Drive to Parmenter Point. Filmed 1 September 2026." and `mailto:aamir@miltonly.com`. The three superseded dated `night.mp4` objects were retired after production served the day URLs. QUEUE item 6 marked done.

**(2) Nav v3 merged.** `91c6ae5` exists nowhere (local, origin, the home worktree's reflog); on the question, Aamir chose the branch head, so `feat/nav-v3 @ 3b56020` (app code `287f8ad`) merged by SHA as `6aac9c9`. One conflict, `scripts/verify/run.mjs`, both sides adding a check: resolved as `footer, catchment, video` (`test-audit-night-1` green). Local build exit 0; production `miltonly-rlsf92eae` serves `6aac9c9`, battery **`PASS · 19 checks · 529 pages · 835s`** (the 19th is Home's `footer`). Confirmed at 390 wide on production with puppeteer (`scratchpad/mc003/probe-nav-390.mjs`): the homepage bar (0 to 67 px), the burger and the CTA are in the viewport before and after a 2,400 px scroll; `/streets/attenborough-terrace-milton` ends in an `m-footer`, 2,176 px tall, 58 links. Nav v3 marked done in QUEUE and HANDOFF-home.

**(3) MC-018 started and built** on `fix/neon-egress @ cce6ee8`, CLI preview `miltonly-2flv2tscv`. The four "stop re-fetching the set" fixes from MC-016: (a) the published and entity slug sets (490 + 963 rows on nearly every render, 18 call sites) sit in Upstash for fifteen minutes (`streetSurface.ts`), dropped by every in-app publication write and by `/api/revalidate` on a `/streets` path; (b) the Neighbourhood and published-hub sets (2,600 reads a window) do the same in `src/lib/hubSets.ts`, read by the street render, the footer, the sold options and the hub-card map, dropped by both hub generators and `/api/revalidate` on a `/neighbourhoods` path; (c) `/streets`, the standing consumer (57 renders in fifteen minutes, 3,414 listing rows each), is ISR at an hour (`○` in the build, `X-Vercel-Cache: HIT` on the preview, the same 688 street links as production) with `DISTINCT ON` on the server; (d) `/rentals` caches its 48 listings, counts and nine aggregates per scope for fifteen minutes. `scripts/test-neon-egress.ts`, 29 assertions, in the prebuild; local build exit 0. **Battery on the preview, `EXPECT_SHA=cce6ee8`: `PASS · 19 checks · 548 pages · 756s`.** Three earlier runs failed on the clock, not the code: the creation cron published 17 pages at 00:01Z inside run one (the sitemap grew 529 to 546 mid-crawl); runs two and three sat past the 12-month window's edge with the hub figures the documented hour behind (production, on `6aac9c9` with no MC-018 code, failed the same `hub-meta` lines at 01:10Z: 5 hubs' sale counts, 2 typicals); the `db2` and `db3` tags and the 22 hub paths were purged on the preview and run four passed. **Waiting on approval to merge.** Not done, by design: proposals 1 and 2 (a `--structure` battery mode, one battery per merge) are process changes for their own prompt; proposal 4 was "not worth it" in the recon. After the merge, measure one battery on `pg_stat_statements` against MC-016's 1,138,193 DB1 rows.

**Also in this session, reported earlier:** Node 20 stops deploying on 2026-10-01 (`package.json` pins `20.x`), and the nightly audit did not commit for 2026-09-14.

Report: scratchpad/reports/MC-022-two-merges.md
