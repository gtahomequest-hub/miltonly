# MC-023
CORE · D:\miltonly · main

**(1) MC-018 merged, and measured worse.** `fix/neon-egress @ cce6ee8` by SHA as `eb3a5e8`; the prebuild failed once on main because the merge checked the two hub generators out with CRLF and `test-neon-egress` had a line-ending-sensitive regex, fixed as `9cbcdb8` (`\r?\n`); local build exit 0; production `9cbcdb8`, battery **`PASS · 19 checks · 567 pages · 781s`**. `pg_stat_statements` reset on DB1 and DB2 before the battery (with the `db2` tag dropped, as MC-016 did) and read after: **DB1 4,241,533 rows against MC-016's 1,138,193**, 3.7×. The two slug sets alone were 3.3 million: the entity set 2,031 calls × 963 rows, the published set 2,438 × 568, against 481 calls in the baseline. Cause: `cached()` skips Redis under a static render (MC-017, the Upstash client's `no-store` fetch is a bailout Next records), and since MC-017 nearly every page is a static render on its first visit, so the sets left Neon on every ISR render, twice, where before they left once per render from one call site.

**The fix is on `fix/neon-egress-2 @ 8279ead`, waiting on approval.** The sets moved from Upstash to the Data Cache: `unstable_cache` under the same keys, an hour (the Neon reads' own revalidate, so no ISR page is cut shorter), tags `surface` and `hubsets` dropped by every publication and hub write and by `/api/revalidate` on a `/streets` or `/neighbourhoods` path; `src/lib/dataCache.ts` wraps it so a local script outside a Next server runs the function (`unstable_cache` throws "incrementalCache missing" there; checked with a script that reads both sets and drops the tag). Local build exit 0, `test-neon-egress` 32 assertions; CLI preview `miltonly-k581rfzw7` serving `8279ead`; **one full battery on it, same method: DB1 263,706 rows, the slug sets at 20 calls each; `PASS · 19 checks · 568 pages · 752s`.** 23 % of the MC-016 baseline, 6 % of merged MC-018. After the merge, measure production once more.

**(2) Node 22 on `fix/node-22 @ 320f325`, waiting on approval.** `engines.node` 22.x. `pnpm build` under Node 22.23.2 (nvm, installed beside 20 and not switched; run through Node 22's own corepack, because the `pnpm` shim on PATH runs the nvm4w Node 20 and reported "Unsupported engine") exits 0, prebuild green, 149/149, no deprecation warnings. CLI preview `miltonly-mijq5oxtm` built on 22 ("Skipping build cache since Node.js version changed from 20.x to 22.x", 1 m 20 s), serves `320f325`; battery **`PASS · 19 checks · 568 pages · 667s`** (a first run failed one equality, `pages read == sitemap URLs: 1177 vs 1178`, because the 02:00Z creation cron published a page mid-crawl). Vercel stops building Node 20 on 2026-10-01.

**(3) The nightly audit: ours, fixed on the same branch.** The workflow fired on schedule on the 14th and the 15th, twice each, but GitHub delivered every run five to seven hours late (13:37 and 14:43 UTC on the 14th, 12:26 and 13:08 on the 15th; runs `34850386523`, `34857529130`, `34968896477`, `34973136768`, all `success`), and the gate kept only a run whose Toronto hour was 03, so the `audit` job was `skipped` every time and nothing was committed. Secrets were never reached. The gate now checks out the branch and runs the first delivery of each Toronto day at or after 03:00 that finds no `scratchpad/audit/nightly/<date>.md`, skipping the rest; four crons, 07 to 10 UTC. Read through the GitHub API with the machine's stored credential (`gh` is not installed). The workflow file is Audit's; changed here because the prompt said fix it if it is ours, and HANDOFF-audit still describes the old gate.

Report: scratchpad/reports/MC-023-egress-node22-nightly.md
