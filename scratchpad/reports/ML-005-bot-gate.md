# ML-005

LEADS · D:\miltonly-leads · feat/leads

1. **Head `064c5b6988f6370dac20ce7b645dfd48a2e503e2`**, one commit on `feat/leads` above the ML-004 docs, `origin/main` `e606d8b` an ancestor. Gated locally, preview pending (Vercel answers 402). Pushed to the branch; nothing pushed to main.
2. **What the bot was.** 64 production `sale-detail` rows from 2026-09-11 10:52Z to 09-16 05:00Z, one lead per Tor exit IP (56 IPs), 62 distinct harvested addresses (62 distinct inboxes after the alias collapse, so the collapse alone catches almost none of it), gibberish two-word names, random `+1` phones, no consent text, and one User-Agent on every row: `"Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_7) … Chrome/142.0.0.0 Safari/537.36"` **wrapped in double quotes**, which no browser has ever sent. Its 64 rows had cost 101 Resend sends (confirmations to strangers' inboxes plus desk alerts) and 104 `LeadActivity` rows.
3. **The door's guard set was already on `/api/leads/create`** (honeypot, origin 403, Upstash per-IP and per-email). It never saw this bot twice, so two things were added to `src/lib/lead/guards.ts`, where the portal door inherits them: **(a)** the per-email limit keys on the collapsed inbox (`emailLimitKey`: plus-tags dropped on every domain, dots dropped on gmail.com and googlemail.com; the stored address is never touched, proven by a gate assertion), with daily windows beside the burst ones (IP 5 per 10 min and 12 per day; inbox 3 per hour and 6 per day); **(b)** `checkUserAgent`, refusing a missing or quoted User-Agent the honeypot's way, 200 with the success body and nothing written. `checkRateLimit` takes its token store as a parameter. The sale-detail form, which had no honeypot (the one form the bot used), carries one now.
4. **`ingestLead` takes `IngestDeps`**: the row, the watch, the confirmation, the desk alert, the SMS, the CRM email, CAPI, the delivery log and the market-pulse read, live by default. `scripts/test-lead-bot-gate.ts` (prebuild) runs 100 bot submissions through the real path with counters, in four cohorts of 25 (honeypot filled; no Origin or Referer; foreign origin; the sale-detail bot exactly): **0 rows, 0 emails, 0 SMS, CAPI, watches or log rows**; 50 answered 200, 50 answered 403. The dotted drip (20 aliases of one inbox from 20 IPs) and the one-IP burst stop at the household's allowance (3 and 5). A control submission reaches the row, the confirmation, the desk alert and the log. Removing the user-agent guard fails it by 25 rows. `[lead-guards] 234`, `[lead-forms] 230`, `[leads-digest] 236`, `[lead-bot-gate] 21`.
5. **What the rule would have refused, last 30 days, replayed over the rows' own timestamps** (`scripts/purge-bot-leads.ts`, dry run; the honeypot and origin checks are not replayed, a row keeps neither). Production, 66 rows: `sale-detail` 64 of 64 (all by the user-agent guard), `rental-detail-book` 1 of 1 (a 2026-08-30 row the old monolith wrote with no headers at all, email `muldoon`; not a browser refusal), `daily-brief` 0 of 1. Preview, 30 rows: 23 refused, all of them the worktrees' own proof batches (30 leads from the desk's IP in an hour on 2026-09-10), by the daily IP window and the inbox window; a proof batch of that size needs the limiter lifted or ten minutes between rows. Without the user-agent guard the listed set (limits and collapse) refuses **0 of the 64** bot rows: the bot never repeated an IP inside ten minutes or an inbox at all.
6. **Purged, `--apply` after the dry run:** the exact fingerprint (source `sale-detail`, env production, User-Agent starting with a double quote, no consent text), **64 rows deleted, 104 `LeadActivity` rows cascaded, 0 `SavedSearch`, `User` or `VisitorProfile` rows referenced**, 0 rows still matching. `public.Lead` is 46 rows. The next leads digest reads 0 `sale-detail` leads for that week and 104 fewer delivery-log rows.
7. **Gates.** Local `pnpm build` exit 0, zero `P2024`. `next start -p 3005` with `VERCEL_GIT_COMMIT_SHA=064c5b6`; the ingress answered the bot's quoted UA `200 {"ok":true}` with no row, no origin `403`, the honeypot `200 {"ok":true}` with no row. `BASE=http://localhost:3005 --only=homepage,nav` exit 0, `064c5b6 served == expected`, no failed assertion. Gated locally, preview pending.
8. **One mistake, reported plainly.** My first honeypot smoke used the wrong field name, so it was a real submission: it wrote one `development` row and sent one confirmation email through Resend (`01a0badf-39ad-733a-b9d9-00a9e9525b08`) to `r.a.y.m.o.n.d.w@gmail.com`, an alias I do not own. The row is deleted; the email cannot be recalled.
9. **Left open.** The portal door does not pass a User-Agent to its guards; wiring `checkUserAgent` into `requestSignIn` is a two-line change in the portal tier. The prebuild rate-limit test on Vercel still spends real Upstash tokens (a known trap); the new daily windows add two keys per submission. A Tor exit list and a name heuristic were considered and not built: both would refuse people.

Report: scratchpad/reports/ML-005-bot-gate.md
