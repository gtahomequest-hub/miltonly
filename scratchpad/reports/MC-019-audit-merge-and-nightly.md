# MC-019
CORE · D:\miltonly · main

## MC-019: feat/audit merged by SHA, the nightly audit run by hand, ignoreCommand proven

- **Merged `feat/audit @ 4a1e349` as `8326b2a`**, by SHA, on `66c3f8e`. 19 files: `scripts/audit/`, `.github/workflows/nightly-audit.yml`, the `.gitignore` carve-out for `scratchpad/audit/nightly/`, the `vercel.json` `ignoreCommand`, the MA-001 report and the baseline nightly report. No app file changed. `c857cbc` on `feat/audit` (docs only) is not merged.
- **Full gate.** Local `pnpm build` exit 0, zero `P2024`, prebuild all green, 609/609 static. Preview `miltonly-jd0eyep7l` (branch alias `miltonly-git-feat-audit`, serving `c857cbc`, same app code as `4a1e349`, built by Vercel with the rule in place): `PASS · 16 checks · 509 pages · 664s`. Approved, pushed. Production `miltonly-71su7errt` serves `8326b2a`: **`PASS · 16 checks · 509 pages · 672s`**.
- **The ignoreCommand rule does not skip real commits.** Locally, `git diff --quiet HEAD^ HEAD -- . ':(exclude)scratchpad/audit/nightly'` exits 1 (build) on the merge commit, a code commit and a docs-only commit, and on a simulated nightly commit that also touches one real file; it exits 0 (skip) only on a commit touching nothing outside `scratchpad/audit/nightly/`. On Vercel: `c857cbc` (docs only) built as a preview; the nightly commit `bbb792f` was **Canceled in 2 s** and production stayed on `8326b2a`.
- **The nightly audit ran once by hand.** Run `https://github.com/gtahomequest-hub/miltonly/actions/runs/34769017742` (`workflow_dispatch`, gate and audit both `success`, 3 m 3 s of runner time). Email **`f97ac935-4da6-443b-b155-7b6606f7428c`** to gtahomequest@gmail.com, subject "Miltonly nightly audit 2026-09-13: 1769 broke, 0 fixed, Lighthouse moved". The runner committed `bbb792f` (`audit(nightly): 2026-09-13`, 2 files) to main.
- **What the first email says, for Core to read.** 4 S2 `treb-string` on four new listings ("Bungalow-Raised", "Backsplit" in visible text) and 1,765 S3, almost all `dead-anchor` (`#type-detached`, `#type-townhouse`, `#type-semi`, `#type-condo` matching no id on street pages), on streets swept for the first time under the three-night rotation. Audit raises these; it does not fix them.
- **Setup done on the way.** `RESEND_API_KEY` and `RESEND_FROM_EMAIL` set as repository Actions secrets from `.env.local` (they were absent). `gh` could not be installed by winget (MSI mutex held by another installer, exit 1618); a portable `gh 2.100.0` ran from the session scratchpad, authenticated with the `gho_` token git already holds (`git credential fill`, scopes `repo, workflow`). Nothing was added to the system.
- **Ownership written.** `CLAUDE.md`: `MA-` task prefix, `D:\miltonly-audit` in the report roots, a "Worktrees and the nightly audit" section (Audit owns `scripts/audit/`, the workflow and `scratchpad/audit/nightly/`; the nightly commit lands on main without a human, pull first; never widen the exclude). `QUEUE.md`: an ownership paragraph at the head and the MC-019 note at the foot. `HANDOFF.md` rewritten.
- **Next, unchanged:** MC-017 (`fix/build-cost`), then MC-015 (`feat/video-playbook`).

Report: scratchpad/reports/MC-019-audit-merge-and-nightly.md
