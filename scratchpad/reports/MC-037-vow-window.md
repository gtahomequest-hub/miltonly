# MC-037

CORE · D:\miltonly · main

Production serves **`2411e8e`** (`fix/vow-window @ 68f6662` merged, one build, one deploy). Gates from exit codes: `pnpm build` exit 0 twice on the shipped tree (578 s, 556 s; 810 static pages; zero `P2024`), all 40 prebuild tests pass (`vow-fields` 111 assertions), `next lint` clean; full production battery **`PASS · 24 checks · 690 pages · 591s`** (`scratchpad/mc003/battery-mc037-prod-2411e8e-run2.log`, exit 0). The first run (`battery-mc037-prod-2411e8e.log`, exit 1) found two things, neither a page defect, both fixed on `main` above the merge as `ecc4524` (its production build is the proof for the validators; no page render reads them): (a) `vow-fields` signs in as the most recently acknowledged verified user, and the Portal builder's MP-006 test row (`gtahomequest+mp006@gmail.com`, acknowledged 19:30Z today) had become that user, so the password sign-in answered 401; the check now prefers the row `VERIFY_PORTAL_EMAIL` names (`.env.local`, beside the password). (b) The regenerated hub prose said "a median of 92 days on market" on bronte-meadows and "median" on old-milton, rural-milton-west and harrison: the voice rule is "typical", never "median", and nothing at hub tier refused it. `validateHubGeneration.ts` and `validateCondoGeneration.ts` fail closed on the word now, the three hub-tier system prompts say so, the four hubs are regenerated ($0.0138) and revalidated, 0 of 22 say it. The check's bare day-count rule is now "its own element or JSON string" rather than six prefixes, because an aggregate can be introduced by any word ("Clarke's 81 days on market"); the JSON keys and "Listed N days ago" are unchanged. **Not fixed:** "average" appears in 15 hubs and 48 condos; the street prompt bans it, the hub-tier prompts do not, CLAUDE.md bans only "median"; about $0.25 to regenerate, your call. Evidence in `scratchpad/mc037/` (untracked).

1. **The blast radius, measured before the cut** (two modellers and two skeptics over DB2 with the page's own pooling; `scratchpad/mc037/journal-results.json`). Published streets 690. **No published street page stops rendering under any bound**: a published street has a `StreetContent` row, and the render gate returns null only when the row, the listings and the sold probe are all absent. Tier changes on **84 pages**: 25 priced-sale → area-only, 12 priced-lease → area-only, 35 area-only → identity-only, 9 priced-sale → priced-lease, 3 priced-sale → identity-only; **37 streets lose their graduated sale typical and 24 their lease typical (58 distinct)**; 59 sale and 37 lease fallbacks print a different figure. Hub ladder: of the 170 rows on the full window, 133 keep a typical (59 re-priced, the largest fifth-line +83%), **37 go silent** on 11 hubs. Hubs: 5 name Q2 2024 and 15 name Q3 2024 in stored prose; a plain 24-month bound would have kept a partial "Q3 2024" of two to seven sales dated 2024-09-23 to 30 in 16 hubs, a label that changes daily and vanishes on 2026-10-01, so the trend starts at the first whole quarter inside the window (Q4 2024 today). Condos: 59 published; 1 names a quarter that ends before the line; "N trades on record" shows on 45 buildings today and **42 after** (1005-nadalin-heights 10 → 7, 1380-main-street 12 → 8, 1487-maple-avenue 15 → 8 lose it). Hero search "N homes": 335 of 772 surfaced streets print a smaller count, **9 lose the suffix**. **Where I stopped, per your rule:** bounding the street existence probe would 404 `walsh-avenue-milton` (a registry street with no page row, one 2024-08 sale) and two debris slugs; bounding `anySaleOnRecord` would make **12 streets state a false "No resales recorded yet"**; bounding `countRecordedTransactions` would stop 8 more streets regenerating. Those three publish no figure, so they stay whole, and the constant's comment and a prebuild assertion say why.
2. **The cut shipped, one constant.** `src/lib/vowWindow.ts` `VOW_DISPLAY_MONTHS = 24` (`null` reverts it; the SQL then reaches back a thousand years). It bounds `streetEnrichment.ts` `fullWindowAgg`, `hubStreetLadder.ts` `rowsFull`, the hub and condo `TREND_WINDOW_MONTHS` (whole quarters), the condo "N trades on record" and `heroIndex.ts` "N homes"; the battery's mirror (`scripts/verify/lib/db.mjs`) carries the same literal and `test-vow-fields` fails if the two diverge or a bound goes missing. DB2 keeps every row. **Regenerated on the deployed prompts and windows:** 22 of 22 hubs (three runs: 20, then willmott, then cobban after two fail-closed rounds on `temporal_pairing`), **$0.1125**; 57 of 59 condos regenerated and 2 skipped by the zero-data guard (158-mill-street, 174-bronte-street, no sale or lease in 12 months, their prose kept), **$0.1120**; hubs, condos and tags purged. Total regeneration **$0.24** with the four median rewrites.
3. **The purge, executed** (`migrations/sold/006_showing_fields_purge.sql`, recorded in the file): **8,650 rows updated** (`showing_requirements` 8,649, `showing_appointments` 1,852, the two `raw_vow_data` keys 8,650); **0 / 0 / 0 remain**. Nine of the 1,852 appointment strings carried an email or phone pattern; nothing was harvested.
4. **The 8.25 sentence, on every page.** `src/lib/vowNotice.ts` holds both sentences; the footer, the street sold-records island, the listing island, `VowComplianceNotice` and `/sold` render them together: "… commercial purpose or any other purpose. The information is deemed reliable but is not guaranteed accurate by PropTx." `vow-display` asserts both on twelve page types.
5. **The three ads landing pages** (`/rentals/ads`, `/rentals/ads/<mls>`, `/sales/ads/<mls>`) carry both sentences in their own footer (they mount no site chrome); the check reads all three.
6. **Homepage lede:** "Every street. Every school. Every answer." The only surface (`src/components/home/mockData.ts`); the meta description, OG, emails and manifest never carried the tagline (two report files quote the old one, not surfaces).
7. **The condo generator:** the ten per-record lease rents and the min/max range are out of the input; `byBed` typicals at k5 (each bucket at k5) replace them; the three condo prompt docs no longer permit a per-trade lease claim; the validator forwards no records, so its lease gate fires on any per-trade claim; the choke already holds the building as the one address. The 57 regenerations above ran on this shape.
8. **Stored FAQs:** **19 `StreetContent` rows** (16 published, 3 draft) carried the legacy "Register for full MLS® access …" answer, a fixed template string from the pre-phase41 path of 2026-04-17, not model prose, unreachable by the cron (attempts 4, needs review) and rendered by no page today. **Scrub was right**: `scripts/scrub-legacy-faq.ts --write` rewrote the sentence in place to "Sign in free to see recent closed sales on this street.", kept the generations, revalidated the 16 pages; 0 remain. The same 19 rows still carry the legacy template's "exact days-on-market per transaction is available to registered users" line, unrendered; a follow-up.
9. Left open: the Archive Data question to PropTx (this cut is the reversible default); the two skipped condos keep prose until they trade; `scripts/regen-condo-local.ts` had a CRLF bug in its env loader (fixed in this commit) and expects `REGEN_ORDER` as a file path.

Report: scratchpad/reports/MC-037-vow-window.md
