# MC-031

CORE · D:\miltonly · main

1. Merged `feat/portal @ 98d2cd7` (MP-002b, the password) by SHA as **`c1caac8`** on `e087209` (the nightly audit), `--no-ff`. One merge resolution, in the merge commit: MC-029's three surfaces predate the branch's rule, so `/api/listings/[mlsNumber]/vow`, `/listings` (the grid's cards) and `/api/auth/saved-listings` now gate through `canSeeVowRecords` (verified, acknowledged, password set) instead of a bare `vowAcknowledgedAt`; `test-portal-door.ts` lists the three files, `test-vow-fields.ts` reads for the call. Then `1a5a24d`: the battery signs in with `POST /api/auth/login` when `VERIFY_PORTAL_PASSWORD` is set locally (no email sent, no signup limit spent), the code door as the fallback.
2. Full gate on Node 22: `pnpm install --frozen-lockfile` (bcryptjs), `pnpm build` **exit 0**, 168/168, `P2024` 0, prebuild `[portal-door] PASS: 126 assertions`, `[vow-fields] PASS: 67`. `prisma migrate status`: 31 migrations, up to date (`20260918120000_portal_password` already applied). Pushed; Vercel `miltonly-adc0bgiih` Ready, `/api/build` answers `1a5a24d439c92fdd1c0b92a4c2c4a1e173dabb44`.
3. The password: the desk row `gtahomequest@gmail.com` carried a hash the preview proof had set with no record of it, so it was nulled and set again through the live card; the new passphrase (30 characters) is in `.env.local` as `VERIFY_PORTAL_PASSWORD`, nowhere else. A sign-in at 13:19Z that was not this desk's touched `lastLoginAt` first (`timestamp` columns read +4 h through the neon driver; the true times are UTC).
4. Acknowledged, no password, on production (`scratchpad/mc003/mc031-proof.mjs card`, iPhone UA, 390 wide): `/api/auth/me` says `needsAcknowledgement:false, needsPassword:true`; `sold-records` answers `canSee:false, needsAcknowledgement:true, 0 records`; the street page renders "Choose a password to finish" with two password inputs and "Save password and see sold prices", no name field, no gate, 0 rows (`mc031-shots/1-password-only-card.png`). Typed and saved: 12 rows in 1.6 s, first `2026-09-04 · 610 Farmstead Drive · $463K · 94% · 84d · Royal LePage Real Estate Associates`; `needsPassword:false`; `passwordSetAt` written.
5. Returning sign-in, fresh browser (`mc031-proof.mjs login`): `/signin?redirect=/streets/farmstead-drive-milton#sold-records` → email → the password step ("Your email and your password"; "First time, or forgot your password? The link signs you in and asks you to set one.") → password → landed on `/streets/farmstead-drive-milton#sold-records`, 12 rows, no gate, no card, 3.4 s from open to rows. Wrong password and unknown address both `401 "That email and password do not match. Try again, or email yourself a sign-in link."`.
6. Purged `db2`, `db3` and the 22 hubs; full production battery **`PASS · 21 checks · 646 pages · 878s`** (`scratchpad/mc003/battery-mc031-prod-1a5a24d.log`), `vow-fields` 15 of 15, the acknowledged half signed in through `/api/auth/login` (`lastLoginAt` moved to 13:38Z, no sign-in email).
7. Deleted the `gtahomequest+mc028@gmail.com` row (`cmu6eiss40000xa8babc2xxo9`, 0 saved searches); `User` holds one row. Its disabled `daily-brief` watch from MC-028 was a Lead-side row and was not touched.
8. URLs for Aamir: `https://miltonly.com/signin` (email, then password, "Email me a link instead"); `https://miltonly.com/streets/farmstead-drive-milton#sold-records` (the card inline, the records after); `https://miltonly.com/terms` ("Signing in and sold data"); `https://miltonly.com/api/auth/login` (the returning sign-in, POST). Screenshots: `scratchpad/mc003/mc031-shots/` (not committed).
9. For the TRREB reply, one line: "Every consumer registers with a username, their verified email address, and a password of twelve characters or more that they choose and we store only as a bcrypt hash; no VOW record is served until the bona-fide-interest acknowledgement and the password both exist, and each sign-in expires within 90 days."
10. Left on production: the desk row with the new password, nothing else. Open, Portal's: the sold table clips at 390; `/rentals` sends `?next=`.

Report: scratchpad/reports/MC-031-portal-password.md
