# MC-035

CORE · D:\miltonly · main

1. **Merged tree `7064c1a`** (`fix/deploys @ 0318f35` merged as `722ad58`, whose production build failed the prebuild; the fix `0b331ae` merged as `7064c1a`), pushed, production serves it (`miltonly-oqglupj4x`, `/api/build` answers the commit, `builtAt 2026-09-21T04:33:59Z`, `env production`). Gates from exit codes: `pnpm build` exit 0 four times (672 s, 519 s on the first tree; 536 s, 507 s on the shipped tree), 794 then 796 static pages, zero `P2024` (run detached from the tool shell with three prerender workers after the desk ran out of memory twice); full production battery **exit 0, `PASS · 22 checks · 677 pages · 746s`** (`scratchpad/mc003/battery-mc035-prod-7064c1a.log`), the full sweep as asked. The first production build (`miltonly-aaa3s0u2h`) failed on my own prebuild assertion (`prerenderStreetLimit(undefined)` read `VERCEL_ENV=production` from Vercel's build shell; the local prebuild runs without `.env.local`); the review had flagged it and I had not acted on it; fixed to name its environments, gated twice more, merged.
2. **Part A.** `CLAUDE.md` "Decisions": DEC-MERGE-CORE-ONLY, DEC-ONE-PREVIEW and **DEC-BATCH-MERGE** (Core merges approved SHAs in batches of two or three, one battery, one deploy; SHAs waiting sit in HANDOFF under "held for the next batch"; the exception: a fix for a live production defect merges and deploys alone, at once). Neither DEC-MERGE-CORE-ONLY nor DEC-ONE-PREVIEW existed in the file before; they are written from QUEUE's ownership rule and MC-034's preview rule. **Cadence, last 7 days** (git first-parent on `main` joined to the Vercel deployments, `scratchpad/mc035/cadence.md`): 24 production builds (3.43 a day), 9 of them docs-only pushes that MC-034's ignore rule now cancels, so 15 carrying code (2.14 a day); 18 merges of 16 tasks; the worst 24 h the 7 MA-007 saw. Under the rule the week would have been **6 to 8 deploys, about 1 a day**: five non-urgent batches (09-14 MC-017 ×2 + MC-015; 09-15 MH-006 + MC-018; 09-16 MC-018 fix + MC-023 + MH-007; 09-17 MH-008 + MC-027 + favicon; 09-18 street-v3 + portal + ML-004) plus two live-defect deploys alone (MC-029 the VOW compliance hotfix, MC-032 the bot writes). This task is itself one batch (one merge, one deploy, plus one failed build).
3. **Part B.** `scripts/verify/lib/sample.mjs`, `--streets=sample` (or `BATTERY_STREETS=sample`): a fixed set of one street per class the checks branch on, rotating within each class by a run counter kept in the OS temp dir per host (34 classes today: each of the four tiers, zero-sales, a clip, the condo pill, lease-heavy, the sub-k 90-day sample, band-entitled, sibling slugs, Town geometry, the minimal template, no hub, the highest-volume street, one street from every published hub so the ladder parity touches every hub); the streets whose **data** changed since the last run on this host (`StreetContent` regenerated, DB2 rows written for the street, a sale that left the 12-month window), capped at 20; a rotating fill to fifty. A template change in the diff touches every street; the fixed set is what stands in for that. Whole-corpus checks are unchanged; the per-page checks compare against the crawled set, hub-page and video judge only sampled streets. Smoke-tested on production: `PASS · 2 checks · 50 of 677 pages (sample) · 23s`. **Not the default (item 6):** the default stays `full`, for two reasons I would rather state than ship around. First, the nightly does not run the battery at all (its own structural sweep reaches each street about every four nights), so nothing would catch a sampled-out per-street defect within 24 h until Audit adds `BASE=https://miltonly.com node scripts/verify/run.mjs --streets=full` to `.github/workflows/nightly-audit.yml`, which Audit owns; that line is the request. Second, after Part C the full crawl renders nothing (677 GETs of prerendered pages), so the renders MA-007 counted are gone either way; what sampling saves is wall time and Chrome. Flip the default when the nightly carries the full run.
4. **Part C.** `src/lib/streetPrerender.ts` `prerenderStreetLimit()` **keyed on `VERCEL_ENV`**: `Number.POSITIVE_INFINITY` when it is `production` (a Git build of `main`, `vercel deploy --prod`), fifty otherwise (a CLI or branch preview, an unnamed environment). `.env.local` carries `VERCEL_ENV=production` (pulled from Vercel), so the local gate prerenders the corpus too and proves the build before the push. **warm-streets (item 9): redundant for deploys, the wrong tool for the rest, not built.** Every street now ships PRERENDER exactly as the 22 hubs do, so a post-deploy walk would do nothing. What still empties the street cache is the tag drops: the street page carries `listings`, `db2`, `db3`, `hubsets` and `surface`, and a drop of any of them (the sold sync at 11:00Z when it wrote, the three analytics jobs at 11:30, 12:00 and 12:30Z unconditionally, every listing sync that wrote, every StreetContent publication through `surface`, the creation cron at 00:01Z onward) makes the next request of all 677 pages a foreground REVALIDATED render. A walk after each drop, as warm-hubs does, would be about 6 × 677 ≈ 4,000 renders a day, more than the tooling renders MA-007 objected to, to spare about two visitor requests a page a day one 2.3 s first byte. The lever is the tags, not a walker: drop `db3` only when a job wrote, take `surface` and `hubsets` off the page through a stamp key as MC-034 did for the rows, and the `listings` tag can go the same way; that is a follow-up, not this task. **Item 10:** the new check `prerender-coverage` sweeps every sitemap street (one GET, concurrency 16) **before the crawl** (a crawl turns a MISS into a HIT), reads `/api/build`'s `builtAt` (inlined by `next.config.mjs` at build) and `StreetContent.createdAt`, and fails production when a street published before the build answers MISS; streets published after the build are excluded by design; REVALIDATED is reported, not failed; on a preview the split is reported and the assertion skipped.
5. **Part D.** `src/lib/bots.ts` and `robots.ts`: `User-Agent: PetalBot / SemrushBot / Amazonbot / AhrefsBot`, `Disallow: /`. Vercel Firewall rule **"MC-035 waste bots"** inserted through `PATCH /v1/security/firewall/config` (active version 1, four condition groups, `user_agent` substring, `deny`), the first rule this project has had. Proven on production: AhrefsBot, PetalBot, SemrushBot-BA, Amazonbot **403**; OAI-SearchBot, Googlebot, bingbot, PerplexityBot **200**. The four were 1,610 requests in the last 24 h and 1,358 a day over 7 days (`scratchpad/mc035/skeptic/metrics-daily-7d.json`). Two things the data adds: OAI-SearchBot was the largest crawler on 2026-09-20 (1,548) but over 7 days it is fourth (3,525) behind Bytespider (11,379, more than the four blocked bots together), bingbot and PetalBot; and "no return" is unmeasured, since DB1 holds no lead with any search or AI referrer. Bytespider, tiktokspider, meta-externalagent and MJ12bot are outside the list you gave and were left alone.
6. **Measure, item 13.** The battery's sweep (677 streets in 114 s, before its crawl) read **HIT 1 · REVALIDATED 676 · MISS 0 · PRERENDER 0 · STALE 0**, against MA-007's 546 / 16 / 3 / 14. The MISS column is the finding: every one of the 673 pre-build streets had an entry. The REVALIDATED column is mine: I ran the standing pre-battery purge (`db2`, `db3`, `listings`, the hubs) before the sweep, and a tag drop turns every prerendered page into one foreground render on its next request. The five streets probed **before** the purge all answered `PRERENDER` on their first request after the deploy (line 8). A clean split needs a fresh deploy with nothing touched first; the redeploy I tried was cancelled in 1 s by the new ignore rule (same commit as the last successful deployment, no diff), which is the rule working. The check now records the split on every run, so the next batch's battery is the clean measurement; HANDOFF says: after a deploy, sweep before you purge, and do not purge at all, the build is fresh.
7. **Item 14.** Build duration: **before 81 s wall, 2 billed minutes (168 static pages, `d068f84`); after 145 s wall, 3 billed minutes (796 static pages, 677 streets, `7064c1a`)**: +64 s, +1 billed Turbo minute, +$0.105 a build. Cost per cycle at the new cadence: about 30 production builds a cycle (one a day) at 3 minutes = **about $9.45**, against the post-MC-034 cadence of 2.14 code builds a day at 2 minutes (about $13.50) and last week's 3.43 a day (about $21.60): the batching more than pays for the longer build, about $4 to $12 a cycle less. Local gate: 8 to 11 minutes a build with three workers, twice a task.
8. **Item 15, TTFB, five streets** (`main-street-milton`, `farmstead-drive-milton`, `restivo-lane-milton`, `mccready-drive-milton`, `hill-street-milton`), first request after the deploy then the second: **cold 0.39 / 0.25 / 0.25 / 0.25 / 0.24 s, every one `PRERENDER`; warm 0.13 / 0.14 / 0.13 / 0.11 / 0.17 s, `HIT`**. MA-007 measured 2.3 s on a MISS; the deploy-time MISS no longer exists.
9. Left on production: the firewall rule; nothing in the databases. Evidence: `scratchpad/mc035/` (scouts, skeptics, dumps, untracked), `scratchpad/mc003/battery-mc035-prod-7064c1a.log`, `gate-build{1..4}.txt`, `mc035-sample-smoke.log`.

Report: scratchpad/reports/MC-035-fewer-deploys.md
