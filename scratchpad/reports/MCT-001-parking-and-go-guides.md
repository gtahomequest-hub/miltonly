# MCT-001

CONTENT · D:\miltonly-content · feat/content-2

1. **Two source-grounded guides, built on `feat/content-2` from `fc897e8` (feat/content merged up to origin/main `e71a7f6`).** `/guides/parking-in-milton` and `/guides/milton-go-train-to-toronto`, both in `GUIDE_DEFS`, so the index, the sitemap and the CollectionPage schema carry them; both link down to published hubs (parking: the 12 hubs the 15 pilot parks sit in; GO: the 7 hubs within 1.6 km of the station, nearest first).
2. **Parking is built from the Town's text and nothing else.** Seven milton.ca pages fetched with curl on 2026-09-11 and stored under `src/data/sources/milton-parking/` with URL, fetch date and HTML SHA-256; `src/data/sources/miltonParking.ts` indexes them and holds `PARKING_FACTS`, the only numbers the guide may state. 59 cited sentences, each rendered with its URL and read date beside it (`GuideSection.cited`, additive); FAQs carry a source; takeaways carry the milton.ca path and date in-line. No fine amounts, no visitor permit, no posted-exception street list, because none of the pages has them.
3. **GO is computed, not copied.** `scripts/gtfs/milton-go.mjs` writes `src/data/sources/goGtfsMilton.ts` from the feed (`20260910145058`, valid 10 September to 27 November 2026, 79 dated services). Weekday: 10 trains to Union 6:00 to 8:30 a.m., 63 minutes, 7 stops; 10 back 3:40 to 7:10 p.m., 60 minutes. Weekend: no train; the 21 bus 21 times a day, typically 90 minutes. Weekday 21: 26 direct, first 3:45 a.m., last 12:25 a.m. after midnight. Fare $12.25 single ride. The one odd date, Thanksgiving Monday 12 October (weekday trains, weekend buses), is stated as the feed carries it. Version and validity dates render beside every figure.
4. **Both pass the content validator, at build time.** `scripts/test-content-guides.ts` is prebuild test 21: 429 visible surfaces, 1,313 assertions, including the negative direction (a weakened bundle, a planted place, a superlative, a fabricated count all fail). `validateContentProse.ts` and every Core file untouched.
5. **`pnpm build` exit 0.** Preview **https://miltonly-jqoy3smu4-gtahomequest-hubs-projects.vercel.app** serving `5e7a3e334a7a06a0ab091699f09f2391ec427713`; `/guides`, both guide URLs and `/sitemap.xml` return 200 and the sitemap lists eight guides. Battery with the full SHA: **12 of 13 checks pass, 481 pages, 128 s**; the one FAIL is `tiles` (four assertions, `chretien-street-milton` sub-k price plus two pages with n12 under 5), and `--only=tiles` against production at `2a89120` fails identically, so it is isolated, pre-existing and the street tier's, not this branch's.
6. **Not done, flagged for Core:** up-links from streets and hubs to the two new guides (MC-003's rules, `uplinks.ts` and the battery's `guide-links` check are Core's). Both static sources age: the feed ends 2026-11-27, the parking pilot is reviewed in 2027; nothing schedules a re-fetch.
7. `HANDOFF-content.md` rewritten and `QUEUE.md` marked in `ed32051`, on top of the proven `5e7a3e3`; this report is the one commit after it, so **head is `ed32051` plus this file**, docs only, the tree under `src/` and `scripts/` unchanged from `5e7a3e3`. Pushed. **Stopped; no merge.**

Report: scratchpad/reports/MCT-001-parking-and-go-guides.md
