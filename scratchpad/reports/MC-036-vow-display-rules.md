# MC-036

CORE · D:\miltonly · main

Production serves **`b814d99`** (`fix/vow-display @ 5040893` merged as `1b18563`, whose Vercel build failed the prebuild guard because the prompt-doc change was still in my working copy; the doc committed and merged as `b814d99`, which built). Gates from exit codes: `pnpm build` exit 0 twice on the shipped tree (638 s, 666 s; 810 static pages; zero `P2024`), all 40 prebuild tests pass (`vow-fields` 100 assertions), `next lint` clean; full production battery **exit 0, `PASS · 24 checks · 690 pages · 368s`** (`scratchpad/mc003/battery-mc036-prod-b814d99-full2.log`; the first full run failed three assertions of the two new checks on their own scoping, a street link in the site chrome, Next's echoed `?page=9` and the word "lock box" in a brokerage's public remarks, all three check-side and corrected before the second run). Method: a four-auditor read of the thirteen items with a skeptic each, five fix packages with a reviewer each, the review findings folded in by hand; evidence in `scratchpad/mc036/` (untracked). The two source PDFs and the README are in `docs/compliance/`.

## Your item list against the source document

Read directly (`docs/compliance/PROPTX_VOW_Best_Practices.pdf`, 13 pages). Items 1 to 4 and 6 to 13 are as you summarised them, with the citations right. Three things the summary did not carry:

- **Item 22 has a second half.** The notice must say both that the site "may only be used by Consumers that have a bona fide interest …" **and "that the data is deemed reliable but is not guaranteed accurate by PropTx"** (live rule 8.25). The recommended sentence you quoted is verbatim in the PDF and now on every page; the reliability sentence is not yet on any page. It reads "on each VOW", not "on each VOW page": a footer on every page satisfies it.
- **Appendix A page 11** is as you listed it: `internet` (Distribute to Internet Portals, Y/N) and `disp_addr` (Display Address on Internet, Y/N), and for `disp_addr = N` the fields that may not be displayed or mapped: Address, Street Number, Street Name, Street Abbreviation, Street Direction, Apt/Unit, Postal Code, Legal Description, Map #, Map Column, Map Row. In the RESO feed AMPRE exposes exactly two of these flags (`$metadata`, read 2026-09-21): `InternetEntireListingDisplayYN` (= `internet`, stored as `permAdvertise`) and `InternetAddressDisplayYN` (= `disp_addr`, stored as `displayAddress`); there is no `InternetYN` property, so nothing is missing at ingest.
- **Appendix B's nine clauses** are (c)(i) through (ix); the live rule 8.09 restates (i) to (vi) as (a) to (g) and adds the AI-system language. Against our `VOW_ACKNOWLEDGEMENT_TEXT` (version 3): (i) relationship, (ii) personal non-commercial, (iii) bona fide, (iv) no redistribution, (vii) the click: present; **(v) PropTx's ownership and copyright acknowledged: absent; (viii) express authorisation for PropTx and other Members to access the VOW to verify compliance: absent**; (vi) says "redistribute" but not scraping, data mining or AI systems (8.09(d) to (f)); (ix) the privacy policy must say Personal Information may be shared with PropTx for auditing: `/privacy` does not. Appendix B(b) also says the Member keeps "the username and current password of each Consumer" for 180 days past its validity; we keep a bcrypt hash, which is what any responsible VOW keeps, but it is not the literal record, for the broker of record to note. Item 25 recommends a 60-minute inactivity timeout; a session is a fixed 90-day JWT with no inactivity limit. Item 28 and 8.13 want an audit trail of Consumer activity; only `lastLoginAt` exists (`VowAccessLog` was MP-003's plan).

**The binding rules** at https://proptx.ca/files/proptx_mls_rules.pdf carry **"EFFECTIVE DECEMBER 2, 2024"** on the cover, not 2026-12-02; the README records that date and maps every R-8xx citation to its live number (R-805 → 8.05(b),(c); R-8.06 → 8.06; R-807 → 8.07; R-8.08 → 8.08; R-8.09 → 8.09; R-8.13 → 8.13; R-814(a) → 8.14.01; R-824 → 8.24).

## The gaps, item by item

1. **Address flag (urgent), FIXED.** Before this task `displayAddress` was honoured on the listing page's address line only. Six DB1 rows and 48 DB2 sold rows carry `disp_addr = N`; the new check found **20 leaks on 22 surfaces** on production before the fix: the active sale `W13805456` on its street page (an inventory card with the full address and a "Listed now" ladder mark linking the listing), `/sales/ads` and `/rentals/ads` landing pages with the address in the title, H1, body and alt (`W13805456`, `W12969090`, `W13645462`, `W13779108`), map pins at the exact rooftop of two withheld leases on `/listings?status=rent`, listing-page JSON-LD coordinates and directions links from the rooftop, the cross street and the Street Intelligence link naming the street, the brokerage's own remarks quoting the address beside "Address on request" (`W13645462`), school and mosque pages serialising whole rows, `/rentals`, `/rent` and the ads index printing raw rows, the condo unit list, the alert email, the saved-listings API, `/sold` printing the street beside "Address withheld", the generator prompt carrying withheld house numbers, and the manual `/api/sync` never requesting either flag. All of it is gated now through one mapper and one placeholder, "Address on request"; a withheld row stays in every count and typical (the rule is about the address fields; `marketWatch/edition.ts` was the one aggregate that excluded them and now agrees with the rest). `permAdvertise` (the `internet` flag) was already gating every public surface; `vow-fields` asserts it, `vow-display` asserts the address flag. Not fixed, decision: a withheld row is still counted on its street's page and in the hero's "Active right now"; the sold-records card (VOW-gated) shows an "Address withheld" row under the street's own heading, which is inherent to a street-based site.
2. **Archive Data (older than two years), NOT CHANGED, your decision.** The oldest sold date held is 2024-04-29; **1,105 of 8,683 DB2 rows** (1,096 advertisable) are older than 2026-09-21 minus two years, and 309 more cross the line in the next 30 days because the sold sync never deletes. What renders them: the street page's graduated fallback (a street under k5 in 12 months takes its typical, tier and counts over the whole record: **170 published streets on the sale side, 102 of them with archive rows; 120 on the lease side, 77 with archive rows**, disclosed as "in the last ~2 years"); the hub ladder's typical for the same streets (170 ladder rows on 24 hubs); the hub prose's quarterly trend (a 30-month window: **15 of 24 hubs name Q3 2024 and 5 name Q2 2024**, all archive today); the condo prose (30 months, 62 archive rows) and the condo "N trades on record" (all-time, 197 archive rows); the street existence gates and the hero search's "N homes" (whole record). Everything else is 12 months or less (the homepage, `/sold`, the market edition, the brief, the 90-day cards, DB3's tables at compute). The cut, if you take it, is one lower bound (`AND sold_date >= NOW() - INTERVAL '24 months'`) in `streetEnrichment.ts:74`, `hubStreetLadder.ts:135`, `buildHubInput.ts:41` (`TREND_WINDOW_MONTHS` 30 → 24) and `buildCondoBuildingInput.ts:44`, then the hubs and condos regenerated; the alternative is the Archive Data agreement and token. Also: DB3 keeps 138 stale `street_sold_stats` rows and 8 `street_monthly_stats` rows from mid-2024 for streets with no sale in 365 days (the recompute never deletes them).
3. **2003 floor, ASSERTED.** Oldest dates: DB1 `listedAt` 2025-04-23, DB2 `sold_date` 2024-04-29, `list_date` 2024-04-18, DB3 2024. `vow-display` fails if any is before 2003-01-01.
4. **100-result cap, FIXED.** Before: `/listings` paged 469 sale rows over 14 pages and 1,200 leases over 34, and serialised 191 and 860 map pins in one response; `/saved` read any MLS list the client sent. Now `/listings` reaches 2 pages of 36 (72), pins are capped at 100, saved listings at 100, the free-text search cannot match a withheld address; `/sold` was already 60 of 90 days and the street cards at most 20. The check asserts pages × cards ≤ 100 and pins ≤ 100.
5. **Bona fide notice, FIXED.** Before: on `/sold` and in the acknowledgement text only, with an Oxford comma; `VowComplianceNotice` reached no page. Now the verbatim sentence is in the site footer on every page that renders the chrome, under the street sold-records card in every state, and in the listing page's VOW island; the check reads nine page types. **Open:** the "deemed reliable but not guaranteed accurate by PropTx" sentence (8.25) is on no page yet; the three ads landing pages mount no footer.
6. **Copyright notice, GAP, needs PropTx's text.** The footer says "MLS® data displayed under the terms of the applicable feed agreements" and "© Miltonly"; **no copyright notice in favour of PropTx exists on any page**, and PropTx's published wording is not in the repo or the PDF. Nothing was invented. When PropTx's text arrives it goes into `HomeFooter`'s compliance block and the two VOW cards, and `vow-display` gets one more assertion.
7. **Brokerage in all views, FIXED where found.** Before: `/sold` and the street sold table set the brokerage cell smaller and muted (8.26 says no smaller typeface); the map card, the `/rentals/ads` teaser cards, the saved cards, the ladder's "Listed now" mark and the two content API routes had no brokerage; `/month` landed on a third line after the brokerage. All fixed; the content API now returns `listOfficeName` and no `listedAt` (a `/v1` shape change: ContentEngine's use of `listedAt` was not confirmed). `ListingBrokerage` renders "Listing brokerage not supplied by the feed" instead of nothing (0 public rows lack a name today). The daily brief names streets, never a listing; the alert email had the brokerage beside the price since MC-029. **Open:** the battery's prominence pass still measures five surfaces; the map card, `/saved`, the teasers and the two signed-in tables are not measured in the browser.
8. **Our contact separate, FIXED.** Every contact card on a listing page (the showing sidebar, the rental booking card, the mobile bar, `AgentContactSection`, the grid's booking modal) now opens with "Contact Aamir Yaqoob, RE/MAX Realty Specialists Inc., not the listing brokerage (…)" and the listing's price left our booking card and mobile bar. **Open:** the valuation capture on the listing page (`AudienceCTA`) carries no line.
9. **Non-MLS sources, FIXED for the one that exists.** `ExclusiveListing` is the only non-MLS source (0 rows today); its cards and detail now carry "Not an MLS listing. Listed exclusively by RE/MAX Realty Specialists Inc." `/rentals/ads` and `/sales/ads` are MLS listings dressed as ads, labelled TREB. 8.29 (searched separately) holds because `/exclusive` is its own page.
10. **Full-access claims, REWORDED.** Fourteen hits: "See every Milton sold price" (`/sold`), "See every closed sale on {street}" and "see every one, free" (street), "Every street. Every sale. Every answer." (homepage), "Register for full MLS® access …" (every school and mosque FAQ, and `generateStreet.ts`), "Every home advertised for lease on the MLS today" (menu), "Browse every active rental" and "See all N active rentals" (rentals), the ads FAQ's "the same MLS® data used by every licensed Realtor". **Open:** `StreetContent` FAQs generated before today may still carry the old "full MLS® access" sentence (a scrub or regeneration; not counted here).
11. **24-hour refresh, ASSERTED.** `/api/sync/detect` runs daily at 10:00Z and stamps `syncedAt` on every row it sees; the newest active `syncedAt` was 7.2 h old at the check; the check fails past 26 h. **Open:** `/api/sync/expire` retires a listing 48 h after its last sync, so a delisted listing can show as active for up to two days; tightening it to 26 h risks a mass expiry on one missed feed run.
12. **PI removal, BUILT.** `/privacy/request` (a form with the lead layer's honeypot, origin and rate guards) posts to `/api/privacy/request`, which emails the desk the request with the standing instruction to remove the information and advise PropTx and the listing brokerage at once, and confirms to the requester with a reference (`PIR-…`); production only, the mailto stays as the fallback; `/privacy` and the unsubscribed page link it; TRESA named instead of REBBA. **Open:** no stored record beyond the two emails (no new table by design), no retention job, no in-app deletion code path; the request is a human process.
13. **Derivative works, PARTLY FIXED, one exposure named.** The street prose is built from aggregates (counts, typicals at k5, ranges at k10, days on market, property types, quarterly trend) merged with Town, school, mosque, GO and parking data whose sources are named on the page, and never reproduces a listing's remarks (0 of 691 descriptions). Two things were record-level: the prompt carried **up to ten individual leased records per street** (house number, rent, list price, days on market, month) and asked the model to cite one to three; and **37 of 691 published descriptions name a leased price tied to a month** (only render-time suppression keeps them off the page). The records and the paragraph are gone from the input and the prompt doc; the choke refuses a house-number address a prompt did not declare as its subject. Not done: the 37 stored rows (a scrub or regeneration), and the condo generator still sends per-record lease rents (unit-less, the building's own address) with a prompt that permits per-trade lease claims at k5; the same treatment is a follow-up. The plain reading: aggregates and merged public data in service of brokerage pages sit inside the "solely for providing residential real estate brokerage services" carve-out; per-record reproduction does not, and that is what was removed.

**Agent-only fields (your addendum), CHECKED and one more source stopped.** `broker_remarks` is null on every DB2 row and `raw_vow_data` carries no `PrivateRemarks` (the July purge held). But the sold sync still requested and stored **`ShowingRequirements` (8,649 rows, e.g. `{"Lockbox","Showing System"}`) and `ShowingAppointments` (1,852 rows)**, co-operating-broker instructions under 8.23(b); no page, route, library or email under `src/` ever read them. The source is stopped (both out of the `$select`, both columns hard-nulled, both keys stripped from `raw_vow_data`), `migrations/sold/006_showing_fields_purge.sql` is prepared and not executed (your gate, as in July), the prebuild guard fails the build if any file under `src/` reads an agent-only field or the sync requests one, and the battery's `agent-only` check reads DB2 and nine surfaces and payloads (the content API with its token) for the field names and the showing vocabulary.

Report: scratchpad/reports/MC-036-vow-display-rules.md
