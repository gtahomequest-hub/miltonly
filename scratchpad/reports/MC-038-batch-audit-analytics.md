# MC-038

CORE · D:\miltonly · main

(The prompt carried no task ID; MC-038 is the next in Core's sequence.) One battery, one push, and one production build I asked for plus one I did not (below). Production serves **`31da4b9`**; `main` is that plus this docs commit. Gates from exit codes: local `pnpm build` exit 0 (822 s, 829 static pages, zero `P2024`), prebuild 40 PASS, `next lint` clean, `pnpm install --frozen-lockfile` exit 0 on the merged lockfile; full production battery **`PASS · 24 checks · 708 pages · 525s`** (`scratchpad/mc003/battery-mc038-prod-31da4b9.log`; 708 of 708 streets PRERENDER; the creation cron has published 18 streets since the 690 bar).

1. **The two merges, by SHA, in order.** `feat/audit @ 6e99a86` as `6d19346` (MA-008's report names no SHA because it is committed with the work; `6e99a86` is the only commit that carries `scratchpad/reports/MA-008-morning-report.md` and is the branch head; nothing under `src/` or `prisma/`), then `feat/web-analytics @ 7cfa4a2` as `f1d4080` (the report names `7cfa4a2` in its first line). **No lockfile conflict:** `main`'s `pnpm-lock.yaml` had not moved since `d068f84`, where MH-009 branched, so the 37 lines merged clean.
2. **The morning report ran from `main` by hand** (`workflow_dispatch` through the API with git's own token; `gh` is not installed): run `35681404348`, gate and report both `success`, 54 s of runner time, `exit 0` after 42 calls; email `01a0c70d-8deb-75c9-9a7d-a020662c314f` to the desk; it committed `report(morning): 2026-09-21` (`31da4b9`, `scratchpad/audit/morning/` only). `REPORT_EMAIL_TO` is not among the seven secrets (the env line printed empty) and the script fell back to the desk address, so it worked; set it if the report should go elsewhere. **What the report says that you should read:** Vercel on-demand $165.55 at day 19 of 30, Build CPU Minutes $139.69 of it, projected $261.40 against the $200 cap, "crossed in 4 days"; miltonly ran 18 builds yesterday; homesly is $64.46 of the cycle.
3. **Two production builds for one push, and the second was my timing.** The batch push built `1fee7f7` (`miltonly-gktwx3ok6`, Ready). I dispatched the morning report 24 s later; its commit landed 90 s after the push, and the ignore rule diffs against the last *successful* deployment, which was still `b4ebcd6`, so `31da4b9` built again (`miltonly-ornbkrkke`, identical code). The rule is doing what CLAUDE.md says; the lesson is in the handoff: dispatch the morning report, or any commit that lands without a human, only after the batch's build is Ready.
4. **Web Analytics, proven on production, not just the preview.** On production Vercel serves the script from an obfuscated base path (`/35ca55a203cd90db/script.js`, `NEXT_PUBLIC_VERCEL_OBSERVABILITY_BASEPATH`), not `/_vercel/insights/`, and the script drops a browser with `navigator.webdriver` set, which is why the first probes saw nothing. With both accounted for, the `view` beacons record `/signin/link?t=FAKETOKEN…&r=…` as `https://miltonly.com/signin/link`, `/streets/main-street-milton?w=FAKEWATCH_xyz&q=1` as `…?w=redacted&q=1`, and the hub as `/neighbourhoods/[slug]`. Tomorrow's morning report should stop printing "awaiting first data".
5. **"average" banned at hub and condo tier.** `findMedian` in `validateHubGeneration.ts` and `validateCondoGeneration.ts` now fails closed on `median`, `average`, `averages`, `averaged` and `on average` (`methodology_leak`); the urban, rural and condo system prompts say why (the typical price is a median, so "average" is wrong, not just off-voice). **Regenerated: 15 of 15 hubs ($0.0592, none failed) and 47 of 48 condos ($0.0972, none failed); 158 Mill Street is the zero-data condo the guard skips, so its one sentence ("No average fee is available") is hand-scrubbed to "typical" in both its content and generation rows.** 0 of 22 hubs and 0 of 59 condos carry a banned word; the 15 hub pages and the two indexes revalidated. Total $0.156 against your $0.25.
6. **millside-drive-milton, hand-scrubbed, nothing spent.** The three rental sentences ("One-bedroom leases … near $1,900 in April 2026 and another near $2,000 in August 2026 …", "Three-bedroom units … near $2,700 in June 2026", and the one after it, "clearing in roughly three weeks", which is a claim on two records) are replaced in both `StreetContent.description` and the rendered `StreetGeneration.sectionsJson` by one sentence: "One-bedroom and three-bedroom units have both leased on the street within the window, too few of either to put a rent figure on." (3 one-bed and 2 three-bed leases in the window, under k5). The page revalidated. Two things worth knowing: the street page already suppressed every numeric sentence at render, so the sentences never reached production; and the failed generation (`attemptCount 5`, 19:47Z) did not overwrite the rendered sections, so the page is the September 13 prose, now scrubbed. Nothing queues millside for another attempt (`StreetQueue` done, the weekly cron looks at pages older than 30 days; it is 8 days old).
7. Held: nothing. Open: the Archive Data question to PropTx; `REPORT_EMAIL_TO`; the build-minute burn the morning report names.

Report: scratchpad/reports/MC-038-batch-audit-analytics.md
