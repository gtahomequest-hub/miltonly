# MC-017
CORE · D:\miltonly · fix/build-cost

**MC-020 closed first.** `13214eb` merged by SHA as `1900c46` (local build exit 0, 609/609, no `P2024`), pushed; production `miltonly-pedwawpmh` serves `1900c46`: **`PASS · 17 checks · 509 pages · 1142s`**. HANDOFF names the `#type-<type>` dead anchors as Street Page v3 change 7 (Home).

**MC-017, on `fix/build-cost` (app code `0e48e18`, the docs commit on top is the head).** Hub, condo, guide and listing pages and the `/neighbourhoods`, `/condos`, `/guides` indexes moved from `force-dynamic` to `revalidate = 86400`; `/listings` (searchParams) stays dynamic. Every write path those pages read from drops them (`src/lib/revalidateSurfaces.ts`: the three listing syncs, the two hub generators, the condo generator) and the three analytics jobs drop the `db3` tag (pulled forward from MC-015); the sold sync already dropped `db2`. The street build prerenders the top fifty by Search Console impressions then active listings (`src/lib/streetPrerender.ts`), the other 459 render on first visit under the page's hour. `scripts/test-build-cost.ts`, 42 assertions, in the prebuild. Local build exit 0, 149/149 static (was 609), zero `DYNAMIC_SERVER_USAGE` lines (were 60).

**Two things it took to make ISR real, both now in the code comments.** Next 14 caches a dynamic route only when `generateStaticParams` exists: the first preview served every page `MISS, private, no-store` as before; an empty one on each detail page fixed it, nothing prerenders. And the Upstash client's `no-store` fetch is a static-render bailout Next records before the client throws, so `cached()` now skips Redis under a static render (`staticGenerationAsyncStorage`, the store `unstable_noStore` reads). Effective revalidate is the smaller of the route's and any fetch's: pages that read DB2 or DB3 serve `s-maxage=3600` (the Neon hour from MC-010, kept for the window edge), a condo page or a 404 serves 86400.

**Measured.** TTFB p50, 7 samples, production before and preview `miltonly-4rpubqh2v` after: hub `/neighbourhoods/timberlea` **2.13 s to 0.13 s**, condo `/condos/33-whitmer-street-milton` **0.90 s to 0.11 s**, guide `/guides/what-milton-neighbourhoods-cost` **0.50 s to 0.10 s**, listing `/listings/W13117744` **0.49 s to 0.10 s**; every page `X-Vercel-Cache: MISS` then `HIT` (`scratchpad/mc003/ttfb-*.txt`, `ttfb.sh`). Vercel build on the Turbo machine: prerender **62 s to 13 s**, wall **2 m 19 s to 1 m 44 s**. Preview battery: first run FAIL on 7 lines, all one cause, the hourly generate cron published 14 streets at 00:01Z (the daily cap reset) mid-run (509 to 523, kept as `battery-mc017-preview-0e48e18-run1-creation-edge.log`); rerun **`PASS · 17 checks · 523 pages · 300s`** (the crawl itself is 2 to 4 times faster on cached pages).

**Not done, a decision.** "Automatic Git deploys off for every branch but main" is a branch condition in `vercel.json` `ignoreCommand`; CLAUDE.md says that command skips only the nightly path, and three worktrees were building previews at once tonight. It changes how every worktree gets a preview (`npx vercel deploy` from the branch). Say yes and it is a one-line change plus the CLAUDE.md sentence.

**Waits on approval to merge.** To merge: `git merge --no-ff <head of fix/build-cost>`, push, `npx vercel ls --prod`, battery with the merge SHA, then `sh scratchpad/mc003/ttfb.sh https://miltonly.com after` for the production numbers.

Report: scratchpad/reports/MC-017-build-cost.md
