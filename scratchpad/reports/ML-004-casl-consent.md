# ML-004

LEADS · D:\miltonly-leads · feat/leads

1. **Merge.** `origin/main` `5ac650b` merged clean as `8b0a17a`, no conflicts. Proven tree **`7eeeb79a17f848ee23e8819a60743ab6568116fc`**, two commits above the merge, ML-003 beneath it; the head is one docs commit above it carrying this report, the handoff and the queue mark. Merge the head. Stop; Core merges.
2. **Every recurring email carries one footer and one unsubscribe.** `src/lib/email/footer.ts` prints the sender, the brokerage, the mailing address from `config.brokerage.mailingAddress`, why the reader is receiving it, and a signed link, in the HTML body and the text body. `src/lib/brief/unsubscribe.ts` is now `src/lib/email/unsubscribe.ts` and signs any watch id; `/api/unsubscribe` is the generic route (GET and RFC 8058 POST), `/api/brief/unsubscribe` runs the same handler for links already in inboxes, and the page names what stops by kind. `listUnsubscribeHeaders` sets `List-Unsubscribe` and `List-Unsubscribe-Post` on the brief, the deal alerts and the leads digest. A sender that cannot sign refuses before its loop; a deal alert that did not go out no longer stamps the watch. The digest's recipient becomes a `SavedSearch` of kind `digest` (found or created per address and environment, `src/lib/digest/recipient.ts`) so its unsubscribe has a row to disable; `/api/alerts/match` excludes `brief` and `digest` by name. `BRIEF_UNSUBSCRIBE_SECRET` keeps its name, so no environment changed and no old link stopped verifying.
3. **The mailing address is the one the site already publishes**, `178 Lemieux Ct, Milton, ON L9E 1E9`, the LocalBusiness address on `/rentals/ads`. If the brokerage's own office address is the one CASL should carry, it is one edit in `config.ts`. Nothing was invented.
4. **`consentText` on all 26 submissions, none backfilled.** `src/lib/lead/finePrint.ts` holds four shared disclosures (alert, reply, valuation, SMS and email); the three surfaces that already snapshotted their own keep them. Each surface renders the constant in JSX and sends the same constant, so the row records the words the visitor saw. Five surfaces that showed no fine print now show one (persona router, mortgage calculator, exclusive inquiry, listing booking modals, the rental booking and question cards); the places and rentals alert strips gained a line and a CSS rule each. `scripts/test-lead-forms.ts` walks every file under `src/` that calls the helper and fails a submission with no `consentText`, a string literal, an opaque payload (`postLead(data)`), or a constant the file never renders; each of the four was proven to fail on `StreetAlertCTA` and restored. `[lead-forms] 221`, `[lead-guards] 209`, `[leads-digest] 232`.
5. **The street card says what the cron does.** `/api/alerts/match` reads new active listings only, so the alert card body, its done copy, the address ladder's watch card, both absence-claim CTA bodies, the mock data and the confirmation line say "listed for sale". The condo card and its confirmation line had the same defect and took the same one-line fix while `CondoCTAs.tsx` was open for its consent text; the done copy also stopped using the `shortName` prop in prose.
6. **Proof sends, preview `miltonly-md810529s` (tree `1721347`).** Two preview watches created for `gtahomequest+ml004@gmail.com`. Brief: Resend `01a0b1d1-847d-773d-bb24-ffe8a2f3f895`, received 00:01:33Z, footer and link in the text body. Deal alert (hub watch, 2 matches): `01a0b1d1-a3cb-7162-b3a4-43e673d8a82e`, received 00:01:41Z; the raw message carries `List-Unsubscribe: <…/api/unsubscribe?w=cmu67143o…&t=…>` and `List-Unsubscribe-Post: List-Unsubscribe=One-Click`, both inside the DKIM `h=` list. Digest to the desk: `01a0b1d1-a990-7555-b802-e31e6a5766c5`, `[preview] Leads digest, week to 2026-09-16`, watch `cmu671fym…` created on the send.
7. **Unsubscribe proven four ways.** One-click POST on the deal alert's link: 200, "The New listings in 1029 - DE Dempsey alert will stop", row `alertEnabled=false`. A forged token: 400. The same token on the legacy `/api/brief/unsubscribe` path: 200. GET on the brief's link: 200, "The Milton daily brief will stop". GET on the digest's link: 200, and the next `force=true` digest run returned `skipped: "recipient unsubscribed from the digest"`. After the unsubscribes, `brief/send?only=` found 0 subscribers and `alerts/match?only=` checked 0 watches. All three preview rows deleted afterwards; the digest row is recreated on the next send.
8. **Gates.** Local `pnpm build` exit 0, zero `P2024`, twice (before and after the subject tag). Head preview `miltonly-3obrz4ffz` deployed with `VERCEL_GIT_COMMIT_SHA` passed so `/api/build` reports `7eeeb79`. `--only=claims,nav,footer,homepage`: claims, nav and footer passed first run; homepage failed `proof-street-pages` 589 against 606 because a fresh preview's Data Cache holds the published set for an hour and only production's writes revalidate it. Purged with `/api/revalidate {path:"/streets"}` on the preview, re-run **PASS**. Production's own homepage check fails `proof-sales-12mo` 1,723 against 1,712 at `10de234`; main's, not this branch's.
9. **The head adds one line above the proven sends:** a non-production deal alert now says `[preview]` in its subject like the brief and the digest do. Built exit 0 and the battery ran on it; no fourth email was sent to prove the tag.
10. **Left for others, recorded in `HANDOFF-leads.md`.** `/api/seo/digest` (Core, behind `ORGANIC_LOOP_ENABLED`) sends with no footer. The rentals alert strip still promises "SMS alert before it appears on any other site". The digest shows 64 `sale-detail` leads in 7 days across 30 listings, 1 to 5 per page; open item 7 is now a pattern, not a day. The `src/lib/digest/window.ts` view basis fix from ML-003 is still Core's migration.

Report: scratchpad/reports/ML-004-casl-consent.md
