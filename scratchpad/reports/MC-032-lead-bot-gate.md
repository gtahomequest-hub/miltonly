# MC-032

CORE · D:\miltonly · main

1. Merged `feat/leads @ 064c5b6` (ML-005, the bot gate) by SHA as **`2510466`** on `e5c7ca7`, `--no-ff`. The door's guard set on `/api/leads/create`: honeypot, origin, a user-agent guard that refuses a missing or quoted `User-Agent` the honeypot's way (200, the success body, nothing written, nothing sent), and the rate limit keyed on the collapsed inbox (Gmail dots and plus-tags, for the key only) with a daily window beside the hourly; every write and send behind `IngestDeps`; `scripts/purge-bot-leads.ts` (applied by Leads on 09-19: 64 rows, 104 activity rows).
2. Three merge resolutions, in the merge commit: `package.json` unions the prebuild line (`test-vow-fields` then `test-lead-bot-gate`); `scripts/test-lead-forms.ts` excludes neither `ListingsCardsClient` (deleted by MC-029) nor `ListingDetailClient` (a real form with the honeypot since ML-005) from the honeypot rule; `ListingDetailClient.tsx` keeps MC-029's imports (no `daysAgo`) and takes ML-005's honeypot props.
3. Full gate on Node 22: `tsc --noEmit` clean, `pnpm build` **exit 0**, 168/168, `P2024` 0; prebuild `[lead-bot-gate] PASS — 21 assertions. 100 bot submissions wrote 0 rows and sent 0 emails`, `[lead-guards] PASS — 234`, `[lead-forms] PASS — 222`, portal-door 126, vow-fields 67. Pushed; production Ready, `/api/build` answers `25104668e3bae470bcc9104e9cbc74cc6a9e99ad`.
4. Quoted User-Agent on production (`scratchpad/mc003/mc032-proof.mjs bot`: a Chrome/140 string wrapped in double quotes, origin `miltonly.com`, source `sale-detail`, a real active MLS, address `gtahomequest+mc032bot@gmail.com`): **`200 {"ok":true}`**, `Lead` count 47 before and 47 after, 0 rows and 0 `LeadActivity` for that address, 0 Resend emails to it (`GET /emails`, checked 8 s later).
5. Normal submission (`mc032-proof.mjs normal`, a plain Chrome UA, `gtahomequest+mc032@gmail.com`): `200 {"ok":true,"lead_id":"cmu9vumzp0000xxbthjyyffep","env":"production"}`; the row: `source sale-detail`, `env production`, `score cold`, `intent buy`, the UA stored; two `email_sent` activity rows; Resend by id: the confirmation "I got your message" to the address, **delivered**, and the ops alert "New lead — sale-detail" to `gtahomequest@gmail.com`, **delivered**; the SMS and CRM paths ran on the production branch of `ingestLead` (not separately observed). Then deleted (`mc032-proof.mjs delete`): the lead and its activity; `Lead` back to 47, 0 rows for either proof address.
6. Purged `db2`, `db3` and the 22 hubs; full production battery **`PASS · 21 checks · 646 pages · 871s`** (`scratchpad/mc003/battery-mc032-prod-2510466.log`), started 14:00Z with no creation-cron collision this time.
7. Left on production: nothing (both proof rows deleted). Aamir's inbox has the ops alert and the desk's phone the SMS for the proof lead, both marked `Mc032 Proof`.

Report: scratchpad/reports/MC-032-lead-bot-gate.md
