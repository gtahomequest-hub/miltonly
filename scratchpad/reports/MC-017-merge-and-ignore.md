# MC-017
CORE · D:\miltonly · feat/video-playbook

**MC-017 merged.** `5e395c9` merged by SHA as `69075d2` (pull first: `main` was `1900c46`, no nightly commit landed today), pushed; production `miltonly-hbmzntrrt` served it, battery **`PASS · 17 checks · 529 pages · 674s`**. TTFB p50 on production, 7 samples, first one cold: hub 2.13 s to 0.18 s, condo 0.90 s to 0.15 s, guide 0.50 s to 0.27 s, listing 0.49 s to 0.15 s, `X-Vercel-Cache: HIT` after the first (`scratchpad/mc003/ttfb-after-69075d2-prod.txt`).

**Git-triggered builds run only on `main`.** `vercel.json` `ignoreCommand` is now `if [ "$VERCEL_GIT_COMMIT_REF" != "main" ]; then exit 0; fi; git diff --quiet HEAD^ HEAD -- . ':(exclude)scratchpad/audit/nightly'`, CLAUDE.md updated. Proven: a push to `chore/ignore-nonmain @ 65e9c90` was **Canceled in 2 s** (`miltonly-iac0jwv82`, "the Ignored Build Step command returned exit code 0"); `npx vercel deploy --yes` on the same branch ran no ignore step, **built and served `65e9c90`** (`miltonly-ey0vbhme3`); the merge to `main` as `3ba3d91` ran the step and **built** (`miltonly-jmcmjk6u0`), production serves `3ba3d91`, battery **`PASS · 17 checks · 529 pages · 698s`**. Worktrees preview with `npx vercel deploy --yes` from now on.

**Build minutes per production build.** Before (last four on `1900c46`'s lineage, "Running build" to "Build cache uploaded"): 2 m 19 s, 2 m 18 s, 2 m 17 s, 2 m 17 s, 609 static pages. After: `69075d2` **1 m 29 s**, `3ba3d91` **1 m 27 s**, 149 static pages. Branch cancels cost 2 s each; before tonight every branch push was a full build.

**MC-015 started and built** on `feat/video-playbook @ 0fd6cb1`, CLI preview `miltonly-4vz89iwdo`, battery on the preview **`PASS · 18 checks · 529 pages · 671s`** (the 18th is the new `video` check: 45 pages, 45 clips, 90 objects HEADed, all 12 assertions 0). Local build exit 0, 149/149, `test-video-playbook` 40 assertions in the prebuild. Live already: **all 47 clips re-keyed** to `streets/<slug>-milton/<YYYYMMDD>/{day,night}.mp4` with the poster beside each (server-side copy, verified, repointed, revalidated; old objects kept for `retire-superseded-clips.ts` after production confirms); the **offset migration** applied to DB1 (28, clean). In the branch: `videoCapturedOffsetMin` / `nightCapturedOffsetMin` and the instant semantics, the sidecar `src/data/streetVideoMeta.json` (47 durations from ffprobe, zero audio streams), `sitemap-video.xml` (45 pages, named from robots), the coverage sentence under every player, the takedown line (`aamir@miltonly.com`), and the upload script's audio refusal, dated keys, offset columns and revalidation. **Waiting on approval to merge `0fd6cb1`.** After the merge, in order: `scripts/backfill-video-captured.ts --write` (47 rows; deploy first, or the three night clips print the UTC date for the interim), `retire-superseded-clips.ts` dry then write, submit `sitemap-video.xml` in Search Console.

**Two decisions taken, both named in HANDOFF.** The offset is two nullable columns because queue item 6 asks for it by name; duration and coverage stayed out of the schema. The dated-key spelling is MC-007's segment (`<YYYYMMDD>/day.mp4`), not the queue's `<YYYY-MM-DD>-day.mp4`, because two clips and `deriveVideoPoster` already used it.

**Not done.** Coverage endpoints for the Milton clips (no `clip-coverage.js` output covers them; dashcam-side work), so every sentence states length and date, never extent. `louis-st-laurent-avenue` and `lower-base-line-west` are published clips with no page, left alone.

**Two things to know.** **Node 20 is deprecated on Vercel: deployments from 2026-10-01 fail** (`package.json` pins `20.x`; every CLI deploy printed the notice). Nothing changed, it is a task. **The nightly audit did not commit for 2026-09-14**; last is `bbb792f` (09-13, by hand). Audit's to look at.

Report: scratchpad/reports/MC-017-merge-and-ignore.md
