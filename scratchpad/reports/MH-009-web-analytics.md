# MH-009
HOME · D:\miltonly-home · feat/web-analytics

**Vercel Web Analytics is in the app, auth tokens redacted before send, previewed, NOT merged.** Branch `feat/web-analytics` cut from `origin/main @ d068f84` (the MC-034 merge), not from `feat/street-v3`. Head `7cfa4a2faaebd30709ab045b7a883fdc841d86d7`, preview `https://miltonly-l47j2kpf5-gtahomequest-hubs-projects.vercel.app` (the one preview, DEC-ONE-PREVIEW; CLI deploy with `VERCEL_GIT_COMMIT_SHA` passed, `/api/build` reports the head). One note for the brief's premise: `origin/main`'s `QUEUE.md` records `feat/street-v3 @ 2553f2e` already merged by Core as `e078e91` (MC-028), so nothing was waiting on it either way.

- **The change.** `@vercel/analytics 2.0.1` (`pnpm add`); `src/components/VercelAnalytics.tsx`, a `"use client"` mount rendered in the root layout inside `<body>` (a function prop cannot cross from the server layout, so the client file owns `beforeSend`). The package's Next wrapper carries its own Suspense boundary around `useSearchParams`, so no static page bails out.
- **The audit, from the code, not a guessed list.** Every `searchParams` read in `src/` was listed, then every URL builder that mints a secret. Three flows put auth material in a query string: the magic link `/signin/link?t=<token>&r=<path>` (`src/lib/portal/door.ts:95`, read by `LinkLanding.tsx`); the one-click unsubscribe `/api/unsubscribe?w=<watchId>&t=<signature>` (`src/lib/email/unsubscribe.ts:74`, read by `unsubscribeHandler.ts`), and the same pair on `/api/brief/unsubscribe`, the path older emails carry; the middleware's preview gate `?preview=<PREVIEW_SECRET>` (`src/middleware.ts:31`). The cron and admin routes read `secret=` (25 routes) and `/api/auth/*` is POST-only; a browser never renders any of them with this script, but the keys are on the list so a pasted URL cannot leak. `/signin`'s own params (`redirect`, `next`, `intent`) are paths and intents, not secrets, and pass through. Vercel's deployment-protection bypass pair (`x-vercel-protection-bypass`, `x-vercel-set-bypass-cookie`) is covered too.
- **The rule, two layers.** A URL on `/signin/link`, `/api/auth/`, `/api/unsubscribe` or `/api/brief/unsubscribe` loses its whole query. Anywhere else, the VALUE of `t`, `w`, `token`, `code`, `secret`, `key`, `preview`, `password`, `auth`, `access_token`, `id_token`, `refresh_token`, `session`, `sig`, `signature`, the two Vercel bypass keys and `_vercel_share`, or of any key containing `token`, `secret`, `passw`, `bypass`, `session`, `apikey` or `api_key`, is replaced with the literal `redacted`. The hash is dropped. An unparseable URL returns `null`. Unit test `scratchpad/mh009/redact-test.ts`, 15 URL shapes plus the `beforeSend` wrapper, all pass (`npx tsx`, exit 0).
- **Proven on the preview, on the wire** (`scratchpad/mh009/probe-views.mjs`, transcript `scratchpad/mh009/preview-views.txt`). The `view` POST body's `o` field is the URL Vercel records: `/signin/link?t=FAKETOKEN_abc123&r=...` recorded as `/signin/link`; `/?preview=FAKE-PREVIEW-SECRET` as `/?preview=redacted`; `/streets/main-street-milton?token=FAKE_TOKEN_999&utm_source=test` as `...?token=redacted&utm_source=test`; `/signin?redirect=%2Fsold&intent=sold` unchanged; `/api/unsubscribe?w=FAKEWATCH&t=FAKESIG` sends nothing (the handler answers raw HTML without the layout, so no script runs). Six views, the endpoint answered 200 to each, `sdkn: @vercel/analytics/next`.
- **The three pages.** `/`, `/streets/main-street-milton`, `/neighbourhoods/timberlea` each posted one view (`dp` `/`, `/streets/[slug]`, `/neighbourhoods/[slug]`). Vercel's project API then reports `webAnalytics.hasData: true` for `prj_XFfVXHeJG1xwqFqi6VwgLy9Gwhk1`; the per-page dashboard is only readable in the browser, so that look is yours. One finding worth knowing: Vercel's script returns early on `navigator.webdriver` or a `Headless` UA, so the battery's puppeteer crawl records no views and spends no events; the probe masks both to behave as a person's browser.
- **Development.** `VercelAnalytics` renders nothing unless `NODE_ENV` is `production`. Probed on `next dev` (`scratchpad/mh009/probe-scripts.mjs`): zero analytics script tags, zero analytics requests. Without the gate the package would inject `script.debug.js` (sends nothing, still a tag). A Vercel preview builds as production, so previews record, which the gates need.
- **Gates.** `tsc --noEmit` exit 0 (after `prisma generate`; the 26 errors before it were the stale client `pnpm add` relinked, none in this change). `pnpm build` on Node 22 exit 0 twice, zero `P2024`, 168 static pages. Battery `--only=homepage,nav` on the preview `PASS · 2 checks · 646 pages · 172s`, exit 0.
- **For Core.** `package.json` and `pnpm-lock.yaml` change (one dependency, 37 lockfile lines, nothing else). MC-034 (`d068f84`) merged today and two more merges are queued, so expect a lockfile merge when this lands. Merge by SHA `7cfa4a2` on approval.

Report: scratchpad/reports/MH-009-web-analytics.md
