# MC-030
CORE · D:\miltonly · prep/mc030

1. **Production is not back.** `https://miltonly.com/api/build` answered 402 `DEPLOYMENT_DISABLED` ("Payment required") on every probe across the hour this task ran (four probes a minute apart at the start, one a minute for thirty minutes after, one at the end); every preview answers the same; `npx vercel ls --prod` shows no new deployment since the cancelled nightly build. Nothing was pushed to `main`: local `main` stays at `c83fffb` (the MC-029 merge `5994cc7` plus its docs), seven commits ahead of `origin/main` `e606d8b`.
2. Prepared and gated locally instead, on `prep/mc030` (pushed to origin as a branch; a branch push builds nothing): `feat/portal @ 51a9bf9` merged as `9c98903` (MP-002b, the password; app code `98d2cd7`; its migration `20260918120000_portal_password` is already applied on the production database, `_prisma_migrations` says so), then `feat/leads @ 064c5b6` merged as `fc05ae0` (ML-005, the bot gate), then `d85a701`. Three conflicts, all in the ML-005 merge: `package.json` (the prebuild line unioned, `test-vow-fields` then `test-lead-bot-gate`), `test-lead-forms.ts` (ML-005's note that the listing page carries a real form and the honeypot, MC-029's deletion of `ListingsCardsClient`), `ListingDetailClient.tsx` (ML-005's honeypot imports, MC-029's dropped `daysAgo`).
3. `d85a701` is the seam between MC-029 and MP-002b: since the password, no VOW record shows without one and `src/lib/vow-access.ts` `canSeeVowRecords` is the one judge, so `/api/listings/[mls]/vow`, `/listings` and `/api/auth/saved-listings` now call it instead of reading `vowAcknowledgedAt`; the island's card asks for whichever step is owed. The battery's `vow-fields` signs in with the returning path first (`/api/auth/login`, `VOW_BATTERY_PASSWORD` from `.env.local`, generated tonight), falls back to the code path, and sets that password through the card route once when the acknowledged row has none. It did so at 18:2xZ on the desk account, and the gate passed; at 21:43Z the same row was acknowledged again (text version 3) and given a different password by another session on the shared database (Portal's local proof, or the desk), so the battery's returning sign-in now answers 401 and the check fails by name: "the account has a password and it is not VOW_BATTERY_PASSWORD". **Decision for the desk:** put the account's password in `.env.local` as `VOW_BATTERY_PASSWORD`, or name a second acknowledged account for the battery; it never acknowledges or resets a row on a person's behalf. The prebuild `test-vow-fields.ts` is at 68.
4. Local gate on `d85a701`, Node 22: `pnpm build` exit 0, 168/168; `next start` on port 3100 with `/api/build` answering the SHA; full battery **`PASS · 21 checks · 626 pages · 647s`** (`scratchpad/mc003/battery-mc030-local-d85a701.log`), `vow-fields` included through the password path. The MC-029 merge alone, `5994cc7`, was gated the same way earlier today (`battery-mc029-local-5994cc7.log`, four hub drifts purged and rerun clean).
5. The three confirmations, on the local build of `d85a701` (the same code production will serve): anonymous `GET /listings/W13800708` answers 200 with 0 VOW-only keys (`listedAt`, `daysOnMarket`, `priorPrice`, `priceChangedAt`, `lastPriceChangeAt`, `soldPrice`, `soldDate`) and 0 day counts in the HTML, and `GET /api/listings/W13800708/vow` answers `{"canSee":false,"needsAcknowledgement":false}` (curl, tonight); the signed-in half was asserted by the 18:35Z battery run on the same build (the gated route answered the facts, the grid's cards carried them, the island rendered "Time on market" in Chrome) and cannot be repeated until the password question in item 3 is settled; the brokerage line matched its price on five surfaces in that run (0 mismatches, measured font size, weight, colour and face). The same three will be rerun on production after the push.
6. **The order when production answers 200**, so the battery reads one build at a time: (a) `git push origin main` (`c83fffb`), `npx vercel ls --prod` to Ready, purge `db2`, `db3` and the 22 hubs through `/api/revalidate`, full battery with `EXPECT_SHA=c83fffb…` (the served commit is the branch head, `5994cc7` plus two docs commits; the app code is `5994cc7`'s byte for byte, `git diff 5994cc7 c83fffb` is three docs files), then the three confirmations on production; (b) `git merge --ff-only prep/mc030` on main, push, Ready, purge, the battery again with the new head, and the confirmations again under the password. The purge and battery commands are in `HANDOFF.md`.
7. URLs for the TRREB reply, unchanged from MC-029's list (`scratchpad/reports/MC-029-vow-compliance.md` item 9): every `/listings/<mls>` (470 active sale and 1,179 available lease pages lose the day count, list date, price history and status; 604 sold, 1,044 expired and 229 leased pages answer the not-available shell, noindex), `/listings` and its `?status=rent` and `?page=N` variants, `/listings?status=sold` (redirects to `/sold`), `/rentals`, `/rentals?neighbourhood=<hub>`, `/rent`, `/rentals/ads`, `/rentals/ads/<mls>`, `/sales/ads/<mls>`, `/streets/<slug>` (626 now), `/`, `/condos/<slug>`, `/schools/<slug>`, `/mosques/<slug>`, every page's Buy menu, `/api/listings/<mls>/vow` (new, gated), `/api/auth/saved-listings`, the deal-alert email, `/listings-v2-preview` (removed); MP-002b adds `/signin` (email and password, "Email me a link instead"), `/api/auth/login` (new) and the VOW card asking for a password on `/sold` and every street's sold-records block. None of it is on production yet.
8. ML-005, for the record: the door's guard set on `/api/leads/create` gains the alias collapse on the per-email limit, a user-agent guard (the bot's quoted UA), and `ingestLead` takes injected deps so `scripts/test-lead-bot-gate.ts` runs 100 bot submissions through the real path; the 64 bot rows are purged. Leads reported it at `064c5b6`; merged by that SHA.
9. Left on the production database by the proofs: the desk account `gtahomequest@gmail.com` has a cleared `verifyCode` and one more sign-in email in its inbox; its password is whoever set it at 21:43Z, not the battery's. The battery's session cookies sit in the OS temp dir per host (a host with a port is now a plain file name; `localhost:3100` had been written as an NTFS stream).
10. Stopped here: `main` unpushed, `prep/mc030` pushed as a branch, both gated locally, preview and production pending. `HANDOFF.md` rewritten on `prep/mc030`; `QUEUE.md` marked.

Report: scratchpad/reports/MC-030-production-return.md
