# MC-034

CORE · D:\miltonly · fix/cost-2

1. **Head `6ba561e`** on `fix/cost-2` (three commits: `f1291a4` the work, `a4c6eb4` the review's two fixes, `6ba561e` the rule under Vercel's 256-character limit), NOT merged. Preview **`https://miltonly-jm3fj4ug7-gtahomequest-hubs-projects.vercel.app`** serves it; full battery there **`PASS · 21 checks · 646 pages · 802s`** (`scratchpad/mc003/battery-mc034-preview-6ba561e.log`). Local gate on Node 22 exit 0, 168/168, `P2024` 0, `neon-egress` 63 assertions. The two approved heads `98d2cd7` (feat/portal) and `064c5b6` (feat/leads) are already ancestors of this branch, merged today as `c1caac8` and `2510466`, so the merge you asked for is a no-op and this battery is the combined-tree battery; the Builders' own reports name earlier heads (`86d5f9f`, `7eeeb79`) and no MP-002b or ML-005 report exists, but the SHAs match the branch heads that were merged.
2. **Item 1, the street render's Listing rows, three layers** (`src/lib/street-data.ts`). (a) `STREET_LISTING_SELECT`, fifteen columns, each read by the render: `mlsNumber` and `address` (the ladder and the inventory card), `streetName` and `address` (the name fallback off `allListings[0]`), `neighbourhood` (hero eyebrow, schema, card route), `status` (every active filter), `permAdvertise` (the ladder gate), `propertySubType` and `propertyType` (the ladder's form, the housing mix), `price` (per-type list average, the card), `bedrooms`, `bathrooms`, `parking`, `listOfficeName` (the card), `latitude` and `longitude` (`computeCentroid`; every row is 0/0 today so it yields nothing, kept at 16 B a row). Dropped: `photos`, `description`, the seven VOW-only columns, `leaseStatus` and 51 others. The card's image is `photos[1]` of the active rows only, one raw query. (b) `getStreetPageData` under `perRequest` (React.cache, exported from `hubSets.ts`): `generateMetadata` and the body share one read per pass. (c) `dataCached` (`unstable_cache`) keyed `["street-listings:v1", <count>:<max updatedAt>, ...siblingSlugs]`, revalidate 3600, tag **`listings`** (`LISTING_ROWS_TAG`, `src/lib/revalidateSurfaces.ts`), dropped by `revalidateListingSurfaces` after every write by `/api/sync`, `/api/sync/detect` and `/api/sync/expire`, and accepted by `/api/revalidate { tag: "listings" }` for the runners. The write stamp is in the key because `unstable_cache` serves a tag-dropped entry stale while it refreshes (the review proved it in Next 14.2.35); a sync changes the key and the next render misses, so no stale listing is served. The tag reaches the street pages too, which now regenerate on their next visit after a sync instead of at their hour.
3. **Measured, per render** (`scratchpad/mc003/rows-cold-then-card.mjs`, tags and path purged, `main-street-milton`, 172 rows): **before, production: 4 full-row pulls, 688 rows, 75 columns** (about 4.9 KB a row on the wire, about 3.4 MB); **after, preview: 2 narrow pulls, 344 rows, 15 columns, plus 2 stamp rows and 2 photo reads of 20 rows** (about 80 KB), and the next request within the hour (the card route) **1 stamp row, 0 pulls** where it was 1 full-row pull. The 4 cold pulls are the HTML and RSC passes of one ISR regeneration running concurrently, each memoised to one read; `cache()` alone took 4 to 2 and the Data Cache takes every later pass to zero. `farmstead-drive-milton` (53 rows): 4 × 53 full rows before, 2 × 53 narrow plus 2 stamps after. Total DB1 rows per cold render barely move (5,823 before, 5,536 to 6,508 after): the other 5,100 rows are the ladder, the slug sets and the context cards, all narrow, the next lever.
4. **Measured, a window** (`scratchpad/mc003/pgstat-window.mjs`, production only, nothing else touching DB1, 21:34 to 21:44Z Sunday): DB1 **16,118 calls, 95,159 rows in 10 minutes**; the street full-row pull **26 calls, 233 rows**, all full-row, so standing street traffic at that hour is about 13 renders in 10 minutes and the batteries and the nightly audit are most of this query's daily count. The after-window is a production measurement and comes with the merge (same script, same hour); on the preview there is no traffic to window. Projection for item 1 at MC-033's 9.4-hour rate (21,828 full-row calls a day, 124,686 rows, about 1.5 GB): the same renders now move about 3% of the bytes cold and none warm, **about 1.4 GB a day off DB1, about 45 GB a month, the 106 GB month-end projection to about 60 GB; dollars $0** (egress is inside Launch's 500 GB allowance), and DB1's compute hours do not move (they are the endpoint being awake).
5. **Item 2, not done, and correctly so.** `lingering-sea-07597558` is **inspectionly.ca's production database**, provisioned by the Vercel Marketplace Neon integration on 2026-04-21 as store `store_yCGewVl7FtIQRIqr` for project `inspectionly-nextjs`: 8 pooled `postgres.js` backends from www.inspectionly.ca at every sample, city and market-report reads every few minutes, 12 daily crons, 20 bookings and 5 leads in its tables. Nothing of Miltonly's connects (every `NEON_*` in `.env.local` names DB2's host; no Vercel env in any other project points at `ep-muddy-dawn`). MC-016 line 14 and MC-033 items 7 and 9 called it unused; both were wrong, and its $15.66 a month is inspectionly's line on the same Vercel invoice, not a Miltonly cost. Delete path, for the record: Vercel Dashboard → Storage only (the org is `managed_by vercel`); not taken. **Suspend timeouts:** `suspend_timeout_seconds` is 0 on DB1, DB2 and the integration endpoint, and 0 means the plan default of 300 s (Neon OpenAPI `SuspendTimeoutSeconds`; the operations log shows 495 suspend cycles on DB1 since 2026-08-18); Launch can only enable that or disable it (-1). **Compute hours saved by setting 300 s: 0 a day, $0.** The computes are awake 24.0 (DB1) and 23.1 (DB2) h a day because traffic and the hourly `/api/sync/generate` (which reaches DB2 through `getStreetStats`) touch them every few seconds; the average is already 0.27 CU. No setting was changed.
6. **Item 3, done** (`vercel.json`, `CLAUDE.md`): a `main` push builds only when it changes something outside `scratchpad/`, `docs/` and `*.md` since the last successful deployment (`VERCEL_GIT_PREVIOUS_SHA`, `HEAD^` when absent, build when the base is outside the clone); `docs/phase-4.1/` always builds because `src/lib/ai/compliance.ts`, `generateStreetDescription.ts` and the hub and condo generators read those prompt files at runtime. The review caught that the first form diffed the tip commit alone and would have skipped the merge under a docs commit, the shape of every task end; proven on the real pushes: `e606d8b..c83fffb` (merge plus two docs commits) builds, `2510466..5bd57e1` (docs only) skips, the nightly audit skips, a missing base builds. Docs-only production builds were 12 of the last 28 ($2.94 in 7 days) and 48 of 113 this cycle: **about $13.8 a cycle saved at the cycle rate, about $9 at the post-branch-rule rate.**
7. **Item 4, previews per worktree, last 7 days** (72 previews: 49 Git of which 34 skipped by the branch rule and 15 built, 23 CLI; skips cost $0, the skeptic reconciled the team's daily Build CPU Minutes): **D:\miltonly 39 billed min $4.10** (9 CLI + 7 Git built across fix/vow-compliance, fix/hub-truth, feat/video-playbook, fix/audit-night-1, fix/build-cost, fix/neon-egress, fix/neon-egress-2, fix/node-22, docs/MC-021-reporting, chore/ignore-nonmain), **D:\miltonly-home 34 min $3.57** (8 CLI + 5 Git built: feat/nav-v3, feat/rent-menu, feat/street-v3, feat/mobile-fixes, fix/favicon), **D:\miltonly-portal 7 min $0.74** (3 CLI: MP-001, MP-002, MP-002b, MP-002c), **D:\miltonly-leads 6 min $0.63** (3 CLI, ML-004), **D:\miltonly-content 6 min $0.63** (2 Git, MCT-001), **D:\miltonly-audit 3 min $0.32** (1 Git built, feat/audit, a tier that reads production). Since the branch rule (5.79 days): 23 CLI previews for 15 code tasks, 1.5 a task, 1.8 among the 13 that previewed; eight tasks previewed twice or three times (MH-005 three in 28 minutes, ML-004 the same fix twice 2 minutes apart, MH-006 the same SHA twice).
8. **Proposed rule:** one preview per task: a task that changes code deploys exactly one preview, `npx vercel deploy --yes` from its worktree, after the local gate (`pnpm build`, exit 0) and the local battery (`next start`, `BASE=http://localhost:<port>`, the db2/db3 and `listings` tags dropped first or the on-disk Data Cache serves yesterday's figures), the way MC-029 iterated under the pause; a second preview needs one sentence in the report naming the check only Vercel can run (an edge route, a Vercel env, the Data Cache, the ignore command), no third; tasks that change no code (merges, docs, recon, audit) deploy none and take the production build as their proof; Git previews stay off. **Saving: about $11.50 a cycle** (previews now about $30.50 a cycle at the post-rule rate, one per code task about $17.90). This task took two, the second because the review's findings (the 256-character limit and the Data Cache's stale serve) are Vercel-only behaviours.
9. **Projected savings per line:** item 1 about 45 GB a month of DB1 egress and $0; item 2 $0 (nothing to delete, nothing to set); item 3 about $9 to $14 a cycle; item 4 about $11.50 a cycle. Together **about $20 to $25 a cycle of Miltonly's roughly $57 build spend at the post-rule rate**, and the Neon bill unchanged at about $37 a month for DB1 and DB2. The remaining Vercel lever is the count of production builds themselves (3.1 a day at about $0.32 each); the remaining Neon lever is the 5,100 narrow rows a street render still reads.
10. Evidence: `scratchpad/mc034/` (the scouts' and skeptics' dumps and notes, untracked), `scratchpad/mc003/rows-per-render.mjs`, `rows-cold-then-card.mjs`, `pgstat-window.mjs`, `pgstat-window-mc034-before-clean.json`; the review's remaining notes (`displayAddress` not honoured on the street card, pre-existing; `townLat`/`townLng` are the coordinates the rows really carry) are recorded in HANDOFF for a later task. Stopped at the preview as asked; merge is yours.

Report: scratchpad/reports/MC-034-cost-fixes.md
