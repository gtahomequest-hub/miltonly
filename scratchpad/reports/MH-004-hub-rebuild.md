# MH-004
HOME · D:\miltonly-home · feat/hubs-v2

**Preview:** https://miltonly-46hylje3o-gtahomequest-hubs-projects.vercel.app
**Hubs:** urban https://miltonly-46hylje3o-gtahomequest-hubs-projects.vercel.app/neighbourhoods/timberlea · rural https://miltonly-46hylje3o-gtahomequest-hubs-projects.vercel.app/neighbourhoods/rural-milton-west · sub-k https://miltonly-46hylje3o-gtahomequest-hubs-projects.vercel.app/neighbourhoods/moffat
**Head SHA: `26af26bb3274ce65427dc7c72efb2ef70023cbbc`**, not merged. Commits after it are docs only. The branch was cut from `feat/menu-v2@ef8d70b` with `origin/main@15835ed` merged in, so merging this SHA lands the menu (`3ec8b51`) too.

- **Nothing static on the page.** The glance is derived facts, each with its basis under the figure and a link to the rows behind it: typical sale price `across N sales in the last 12 months` (sub-k: the sale count, "below the publication floor of five"), streets with a page, streets filmed, schools inside the Town boundary (by position against the Town polygons), homes for sale today, and the dominant housing form as a share of sales. `suits` / `commute` / `schools` prose is gone; a fact with an empty source is not rendered. The sibling cards lost their hand-written character line and are now the four NEAREST hubs by the Town's polygon centres, distance printed to 0.1 km, with typical, sample and street-page count; the four rural hubs with no polygon get "Other rural neighbourhoods" and no distance.
- **The ladder is every published street** (48 on Beaty, 1 on Moffat), ranked by 12-month sales, each row carrying the street page's own k-gated typical, its disclosure line, "sample too small to publish" below k, and the sold count always. `src/lib/hubStreetLadder.ts`: two grouped DB2 queries, pooled on `deriveIdentity(slug).identityKey` (the identity `resolveSiblingSlugs` keys on), graduated 12mo then full record at k5, rounded through `roundPriceForProse`. Three rungs, film first: the strip of every filmed street in the hub, the ladder marking them, the A-to-Z index (the overflow page above the cap, `/streets` below it). Intent squares: buying `/listings?neighbourhood=<token>` with the live count, selling `/value/<slug>`, renting `/rentals`, investing `/sold?nbhd=<slug>` with the sale count. Guides stay linked up (MC-003). The hub ends in the homepage footer, whose two redirecting links are replaced (`/map` to `/market-watch`, `/book` to `/guides`), which also changes the homepage.
- **Gate:** `scripts/verify/checks/hub-page.mjs`, the 15th check, 18 assertions. Every `data-fig` outside the site nav must have a declared source, format and tolerance (an undeclared figure fails); the ladder must equal the hub's published street set; each row's typical AND basis must equal the street page's hero tile as rendered in the same crawl (480 rows, 0 differ); every internal href must return 200 with no redirect and every fragment must match an id (711 targets); JSON-LD must parse and its ItemList must count the ladder. `loadHubRecord()` grew `hubPage(slug)` and the Milton-wide typical. `guide-links.mjs` reads the rebuilt condo-section marker. Contrast: `.hh-glance` needed `.hh-sec.hh-glance` specificity (the generic cream ground was winning and painting white figures on cream); small text on dark grounds sits at 0.62 white; `probe-hub-contrast.mjs` reports nothing under 4.5:1.
- **Build and battery.** `pnpm build` exit 0, zero `P2024`, 24/24 prebuild, 581 static pages. Battery on the preview at `26af26b`: **`PASS · 15 checks · 481 pages · 547s`** (and `PASS · 15 checks · 481 pages · 555s` at `9597c9b` before the footer-spacing commit). One earlier local run failed on figures alone: DB2 moved under the hour caches during the run (Beaty 187 to 185 sales, Milton 1,736 to 1,727), and `hub-meta` (Core's check, untouched) failed identically, the MH-001 moving-count trap. Re-run green once the caches turned over.
- **Open, owned elsewhere:** the overflow page `/neighbourhoods/<slug>/streets` is now a sorted duplicate of the hub's own ladder above the cap (keep as the A-to-Z rung or retire, Core's call); `/rentals` takes no neighbourhood filter so "I'm renting" is Milton-wide; the hub `<title>` and meta description carry em-dashes from `hubMeta.ts`, the shared formula `hub-meta.mjs` parses. Screenshots at 1440 and 380 are in `scratchpad/hubs-v2/` (untracked). Stopped; no merge.

Report: scratchpad/reports/MH-004-hub-rebuild.md
