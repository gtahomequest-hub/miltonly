# MH-008
HOME · D:\miltonly-home · feat/mobile-fixes

**Aamir's phone review of 2026-09-16, five items and a 390 gate, previewed, NOT merged.** Head `dd1118febafcbfd942050c4d7c7c6823e92f962c` (from `origin/main@3ad3c0a`), preview `https://miltonly-30qag5057-gtahomequest-hubs-projects.vercel.app`, `--only=phone-390,footer,nav,homepage,hub-page,guide-links` on it: `PASS · 6 checks · 589 pages · 599s`. Local build exit 0, zero `P2024`; the same six checks local `PASS · 6 checks · 589 pages · 631s`.

1. **Listing detail page on the theme.** The navy and orange Tailwind palette in `ListingDetailClient.tsx`, `ListingExtras.tsx` and `page.tsx` is mapped onto the site's literals (deep `#073126`, accent `#017848`, CTA `#00ff80`, cream `#fffdfa`, the muted greys of `site-chrome.css`); on the deep-ground cards the muted text is `white/60` and the accent is the CTA green; the "See sold prices" card is forest with a green link; amber and blue badges are forest tints; reds and greens stay as status colours. Headings were already Fraunces through `site-chrome.css`. Both grid columns are `min-w-0`: the body ran to x=422 at 390 (remarks and detail values clipped) because a grid item's min-width is auto.
2. **Investor block.** Gone. `TypicalRentBlock` states the Board's k-gated typical rent for this home type from `getLeaseMarket()` (`rentSignals.ts`, the Rent menu's figure): whole home and basement unit apart where the basement cleared k5, each with its window and sample, and a basis line; it renders only when the whole-home figure clears the floor. No cap rate, no cashflow, no assumed rate, down payment or maintenance; the average asking-rent query that fed them is deleted. No em-dashes remain in the three files or the page's title and description; no money is formatted from a signed number.
3. **Cookie banner.** One sentence in the site tokens ("Cookies for site analytics only, under PIPEDA. Privacy"), forest ground, Decline outlined, Accept in the accent; the "personalize content" claim is gone. While it is up it sets the body's `padding-bottom` to its own height (ResizeObserver), so the footer's brief field scrolls above it on a phone. It wraps to two rows at 390.
4. **Footer band.** `.m-fforms { flex: 1 1 340px }` is a 340px flex-basis, and once `.m-ftop` stacks into a column at ≤780 that basis is HEIGHT: the forms block reserved 340px and the empty band sat under the brief field. `flex: 0 0 auto` on the phone, and the stacked padding and margin (106px) brought to 42px.
5. **Street CTA card and neighbours.** `.s-finalgrid > * { min-width: 0 }`, the alert row wraps and the email field takes its own row at ≤560, the block and cards give back their desktop padding on a phone (the alert card ran to x=443 because the email input beside a no-wrap button set the grid track wider than the screen). `buildFinalCTAs` and the sidebar eyebrow and the corner insights use the resolved `streetName`, never `shortName` ("Selling on Main" is "Selling on Main Street"). The alert body is "An email when a home on <street> is listed or sold. Nothing else, and no account." `guide-uplinks.css`: every rule carries the block class, (0,2,0), because `.street-v2 *` and `.hub-v2 *` reset padding and margin at (0,1,0) after it and had zeroed the block's side padding and the rows' rhythm (the GUIDES eyebrow at x=0). Compare teaser headings: "Freehold vs. condo, side by side". The sold gate's eyebrow and button are `--s-green`; `--s-gold` is retired. The records table scrolls inside its card (`overflow-x: auto`) instead of being cut at the card's edge.
- **Gate.** `scripts/verify/checks/phone-390.mjs` (20th check): a real browser at 390 by 844 on the home, the first street, the first hub, the first listing, `/rentals` and a guide; the document must not scroll sideways and no visible text node's box may pass the viewport (a horizontally scrollable container, an `aria-hidden` wrapper and an SVG are allowed to). It caught the records table before this branch fixed it. Registered in `run.mjs` and the README.
- **Outside Home's files, flagged:** `src/lib/street-data.ts` (names in headings, the alert body), `src/components/street/v2/street-theme.css` and `mockData.ts`, `src/components/guides/guide-uplinks.css`, `src/lib/comparisonData.ts` (teaser titles), `src/components/consent/ConsentBanner.tsx`, the three listing files. Each change is the item that names it.
- **Seen, not done:** the listing page's own fixed bottom bar (`MobileBottomBar`) also covers the footer on a phone and now sits under the cookie banner until it is dismissed; the mortgage calculator keeps its slider assumptions and flat maintenance and insurance figures under "Estimates only". Neither was in the brief.

Core merges by SHA on approval.
