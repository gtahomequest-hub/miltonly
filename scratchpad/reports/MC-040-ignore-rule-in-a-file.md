# MC-040

CORE · D:\miltonly · main

The ignore rule is a file now. `vercel.json` says **`"ignoreCommand": "bash scripts/vercel-ignore.sh"`** (29 characters, from 250 against Vercel's 256 limit). Production serves **`bfc389b`**. Gates from exit codes: `npx tsx scripts/test-vercel-ignore.ts` exit 0, **12 assertions**; `bash -n scripts/vercel-ignore.sh` exit 0; no local build and no battery, since nothing under `src/`, `prisma/` or `public/` changed (the two Vercel builds below ran the full prebuild chain, 42 tests, and both exited 0).

1. **`scripts/vercel-ignore.sh`.** Same decision as before plus `':!scripts/audit'`: skip when the range touches only `scratchpad/`, `docs/`, `*.md` or `scripts/audit/`, build otherwise. The two guards you named are kept and are written into the file as the reason it exists. The range is `VERCEL_GIT_PREVIOUS_SHA`, falling back to `HEAD^` when it is absent **or set-but-empty** (`${VAR:-…}`, which Vercel does send), never the tip commit alone. `docs/phase-4.1/` is checked first and always builds, because `compliance.ts` and the generators read those prompt files at runtime. **Everything uncertain builds:** no `git` on PATH, not a repository, no repository root, a base or head the clone does not carry (a shallow clone, a force-push), or a `git diff` that exits anything other than 0 or 1. The script prints one line saying BUILD or SKIP and why, and that line is now in the Vercel log for every deployment.
2. **Runnable by hand:** `bash scripts/vercel-ignore.sh <base> <head>`; `echo $?` gives 0 for skip, 1 for build. With no arguments it judges the same range Vercel would.
3. **`scripts/test-vercel-ignore.ts`, 12 assertions, in the prebuild chain.** Your seven cases, each on real SHAs: `1cb431e..b4ebcd6` a merge whose tip commit is docs-only → BUILD; `17faadc..b3bced3` scratchpad only → SKIP; `c1565b7..4ffad6e` `*.md` only → SKIP; `f50bfba..c1565b7` (the MA-008 addendum, the push that cost MC-039 a build) `scripts/audit/` only → SKIP; `320e406~1..320e406` `docs/phase-4.1/` only → BUILD; `6524a20~1..6524a20` `src/` only → BUILD; an unreachable base → BUILD. Five more: an unreachable **head** → BUILD; **both halves of the MC-034 regression**, `b4ebcd6~1..b4ebcd6` (the tip alone) SKIP against `1cb431e..b4ebcd6` (the true range) BUILD, so the naive rule's mistake is a standing test; a `src/` range on a non-main branch → SKIP and the same range on main → BUILD, proving the branch gate is what decided it; and `vercel.json`'s `ignoreCommand` is still the one this script backs. **A fixture the clone does not carry is reported and not counted, never failed** — on Vercel's shallow clone four are unresolvable and the build passed with the other eight; here all twelve run.
4. **Proven on Vercel, both directions, with the rule live.**
   - `ad8ae77` the rule itself → **`miltonly-a9a4yw0ow`, Ready** (log: `[vercel-ignore] BUILD: vercel.json …`). The new rule governs the very deployment that introduces it, since Vercel reads `vercel.json` from the commit it is building.
   - `886249e` one marker file under `scripts/audit/`, the whole push → **`miltonly-4w5ocqrfw`, Canceled**. The log reads `[vercel-ignore] SKIP: ad8ae770…..HEAD touches only scratchpad/, docs/, *.md or scripts/audit/` then "The deployment was canceled because the Ignored Build Step command returned exit code 0." The same shape of push built the whole site yesterday (`miltonly-gepjcuv36`).
   - `bfc389b` the next real code push, `package.json` plus the marker's removal → **`miltonly-mk82xhesq`, Ready** (`[vercel-ignore] BUILD: package.json`). So a skip does not poison the next build: Vercel's previous-SHA stays at the last **successful** deployment, `ad8ae77`, and the range from there carried `package.json`.
   - Nothing of Audit's is left changed: the marker went in and out in the two commits above, and `scripts/audit/` is byte-identical to `9603468`.
5. **Two things I did not change, both on purpose.** `.github/workflows/` is still not excluded, so a workflow-only push builds; you said keep the behaviour identical apart from `scripts/audit`, and that one is arguably right to build anyway. And the branch gate still skips every non-main push, which is what sends a worktree to `npx vercel` for its preview.
6. The script's blob is LF (checked: zero carriage returns staged), so Vercel's Linux shell reads it; it is invoked through `bash`, so no executable bit is needed on Windows.

Report: scratchpad/reports/MC-040-ignore-rule-in-a-file.md
